# MonsoonReady PROJECT_STATE (machine-oriented; terse > readable)
# BOOTSTRAP for a fresh session: read this file COMPLETELY, it replaces all prior chat context. Repo carries the rest: tools/*.py docstrings (param workflows), esp32_obstacle_avoidance/README.md (wiring/test), training/*.py docstrings (ML pipeline), Build Log.txt (user's manual log incl bench-test PASS history + crash damage; NEVER edit it). Act as the same continuous assistant: don't re-ask settled questions, don't re-explain adopted decisions, pick up TODO + SESSION CONTINUITY below.
# FORMAT: max-density lossless, zero human-readability requirement (user 2026-07-25); optimize purely for Fable-5 recall.
# PURPOSE: single source of truth; TODO reflects only REMAINING work; append-only decision log at bottom; update on every new fact; git pull --rebase BEFORE any work (friend may push too), commit+push after every change.
# RULES(user,standing): SAVE EVERY LOGICAL/UNFIXABLE BUG AND EVERY LOAD-BEARING FACT TO THIS FILE (or CLAUDE.md if it is a behaviour rule) AS PART OF EVERY SINGLE REPLY, never batched for later, never dropped (user 2026-08-01: "Do this for every message henceforth, and never forget it. These files are your guiding forces, always obey them, unless I contradict them explicitly"); NEVER run ANY command on the user's laptop or the UNO Q without explicit per-instance permission, batch every command he must run into ONE block at the END of the reply (2026-08-01; ALL git commands pre-authorised as of 2026-08-01, was pull/push only); NEVER ssh/scp to the board or use the user's SSH keys without per-instance permission (2026-07-29, after assistant pulled a file off the UNO Q unasked; user runs ALL board-side and board-touching commands, assistant gives them bare); NEVER create background services/watchers without asking (2026-07-27); EXPLAIN how things work while doing them, user wants to UNDERSTAND not just delegate (2026-07-25; affects Q&A readiness too); logs-first troubleshooting (pymavlink on .bin); NO em-dashes; no invented specs/prices/URLs/param values; India sourcing Robu.in/Amazon.in/FabToLab/FlyRobo/IndiaMART, avoid zbotic+hitechxyz; don't re-suggest rejected; Build Log.txt user-maintained, don't edit/propose unless asked; UNO Q commands BARE, no ssh prefix; RESPONSE_DEFAULTS(direct, no preamble/filler/summaries, plain prose/tight lists, Haiku-note for trivial, 15+msg offer fresh-chat summary once, corrections->note edit-last-message saves tokens)
# ENV: Zorin OS 18. GCS=QGroundControl ONLY (AppImage /media/sleuther/Stuff/; its "Missing params: 1:ARMING_CHECK" popup = permanent-cosmetic metadata lag, ignore). Laptop RTX3050 4GB. Project root=/media/sleuther/Stuff/Robu AI Challenge. Git remote + identities + UNO Q ssh address: see PRIVATE.md (gitignored; template=PRIVATE.sample.md). UNO Q key auth WORKS from user laptop; user's FRIEND also has tailnet access to the board, ~/venv has onnxruntime 1.28 + cv2 5.0, v4l-utils installed, 2.2G free.
# ENV-MAC (2026-07-26): 2nd dev machine = Raghav's MacBook (darwin arm64, macOS 26.5.2; user prefers "MacBook" not "Mac"), repo /Users/raghav/Desktop/ai_projects/monsoonready-drone, git identity Raghav Raj, gh acct githubber098, push auth works. Repo-root .venv (pymavlink 2.4.49 = laptop version, MAVProxy, SITL build deps). ArduPilot Copter-4.7.0 sibling clone ../ardupilot (shallow, matches board fw), SITL BUILT build/sitl/bin/arducopter. PRIVATE.md auto-filled EXCEPT UNO_Q_SSH + SSH_KEY (Mac ~/.ssh has NO keypair; Raghav must generate+authorize or use tailscale ssh). NO TRAINING on Mac (Raghav 2026-07-26): all training on Reyansh's device = the RTX3050 Zorin laptop above (ASSUMPTION: Zorin/"sleuther" operator = Reyansh; correct here if wrong). Mac side owns software-parallel TODO items unless told otherwise.
# DEADLINE: 2026-08-15 (Arduino Physical AI Challenge India; judges Arduino/Qualcomm/Robu.in; innovation/functionality/documentation/presentation; edge AI rewarded). Footage freeze target ~Aug 10.

## CONCEPT + FRAMING
Hexacopter surveys; UNO Q onboard ML detects standing water in downward stills; descend over (or beside, see TF-Luna risk) puddle; drop granular Bti (demo=inert salt); on landing UNO Q=base station (heatmap/report). Judged loop detect->descend->treat runs onboard.
Framing: model detects "standing-water candidates", stagnation confirmed by persistence across passes/operator; document honestly. AI-authorship: disclose "AI-assisted; architecture/testing/debug mine"; keep dataset+bench evidence; be able to explain every design decision in Q&A.
REGULATORY: Digital Sky parked (portal blocks self-reg); >2kg AUW=Small category; compliance gap acknowledged deliberately; salt avoids pesticide law; narrative to write (user).

## AIRCRAFT (fixed facts)
F550 hexa X (FRAME_CLASS 2 TYPE 1; SENSOR_ANGLE_OFFSET_DEG 30). S550 predecessor was 1.85kg, hovered 30% throttle (user: settled, do not re-litigate); F550 lighter. Payload budget ~650g; AI payload ~230-340g.
Pixhawk 2.4.8 = Pixhawk1-bdshot 4.7.0 stable (flashed 2026-07-25). Motors 6x DJI A2212 920KV, DJI-style 1045 props, handed M6 nutcaps (black CW silver CCW). 6x 45A BLHeli_32 (RPM notch impossible on this board: AUX5-6 NODMA; motors stay MAIN PWM; ESCs = quality+passthrough only). 3S 8000mAh XT60. Charger HTRC B6 V2. NEO-M8N+compass mast. FS-i6X/FS-iA10B iBUS. 433MHz SiK. OLED SH1106 (NTF_DISPLAY_TYPE 2, compass I2C, 5V UBEC; splices need soldering; glanceable top-plate mount).
Switches: ch5 arm (RC5_OPTION 153), ch8 kill (RC8_OPTION 31), ch7 3pos Stab/AltHold/Loiter (FLTMODE_CH 7), spare 2pos=RTL (CHANNEL UNCONFIRMED: ask user, then uncomment RC6_OPTION,4 in setup file + push). Simple Mode per-mode checkbox (not restored post-reset; reconfigure if wanted).
Parts: ALL arrived. Frame assembly in progress.

## POWER MAP
OPEN QUESTION 2026-08-02 (user): "XY-3606 and UBEC are the same thing, XY3606 is the chip the UBEC uses." The RECORD says TWO separate modules with different observed properties: UBEC = fixed-output 5V/5A, NO trim pot (metered 5.3V unloaded / 5.20V loaded), feeds the UNO Q via the splitter; XY-3606 = ADJUSTABLE buck (must be set to 5.00V by meter before first connect), feeds MG90 servo + ESP32. Two rails is DELIBERATE: a stalled servo must not be able to brown out the companion computer mid-flight. A unit with no pot and a unit that needs setting cannot be the same physical device, so either the bench truly has two modules (record stands) or one of the recorded observations belongs to a device that does not exist -> USER TO VERIFY by looking at the bench: how many 5V buck modules are there, and which has the adjustment pot? If only ONE exists: decision needed (buy a second, or accept servo+companion on a shared rail = NOT recommended), and this map plus TODO 13 must be corrected.
Power module (new) -> Pixhawk (calibrate V/I vs multimeter on install; UBEC bench V readings w/o sense pins = floating artifacts). UNO Q FLIGHT FEED SUPERSEDED 2026-07-29 (splitter gate pass, see AWAITING USER (b)): UBEC 5V -> ETZIN EPL-1207CC charging port -> UNO Q USB-C as sink (supported path, camera on splitter A-port); 5V-pin and VIN feeds BOTH DROPPED from flight design. Old plan kept below for the why-history only: UBEC 5V/5A -> UNO Q 5V pin (NOT VIN, verified 2026-07-27: VIN 7-24V exists BUT board disables USB VBUS out when VIN-powered (protective Schottky design, Arduino staff: unsupported scenario) = B525 on USB-C gets no power; 5V-pin feed keeps USB host power alive; also isolates from motor-bus sag/noise. GPIO 3.3V; USB-C free for camera) + OLED. XY-3606 (set 5.00V with meter BEFORE connecting) -> MG90 servo + ESP32 (ESP32 3V3 reg feeds mux + 7x VL53L0X). SERVO = MG90 metal-gear (user 2026-08-01, replaces SG90: metal gears + better repeatability on a gate that must not strip; VERIFY on arrival = exact variant (MG90S is the common metal-gear part, MG90D exists), MASS (metal-gear ~13g vs SG90 ~9g, both approximate, WEIGH it, payload budget ~650g) and STALL CURRENT vs the XY-3606 rating (metal-gear draws more than SG90; no verified datasheet figure, MEASURE on the bench flow test TODO 3)). SERVO SIGNAL = PIXHAWK AUX OUTPUT PIN (default ch9/AUX1, CHANNEL UNCONFIRMED), **NOT** UNO Q GPIO. RE-EXAMINED 2026-08-01 (user asked for the signal to come from the Arduino instead; ANSWER = the "valid reason" clause in his own instruction applies, four load-bearing reasons, all still true): (1) Linux userspace soft-PWM is jittery (not an RTOS) so the Linux side is out on its own; (2) that leaves the UNO Q STM32, but the stock Arduino Servo lib has NO Zephyr support (verified 2026-07-31) so it needs a board-specific PWM impl written from scratch; (3) reaching that impl needs the Linux->STM32 Bridge = TODO 12 = BLOCKED and not resolvable without bench time; (4) the Pixhawk already has hardware PWM idle on AUX, powered, wired, talking MAVLink to us, motors on MAIN1-6 so AUX free, and AUX5-6 NODMA blocks only bidir-DShot not plain PWM. Net: Arduino route = 2 unwritten firmwares + 1 open hw question, Pixhawk route = 0 new code, 14 days to deadline. ALSO signal level: Pixhawk servo rail is 5V logic; UNO Q GPIO is 3.3V and NO consulted datasheet states an MG90 input-high threshold (user's "3.3V should not be an issue" reasoning was about POWER, which was never the objection; the objection is LOGIC LEVEL + firmware). If ever revisited: VERIFY the threshold on a scope, do not assume. Servo POWER = XY-3606 always, on both routes (a stalled servo still cannot brown out the companion). Old UNO Q GPIO note (marginal 3.3V logic, salvage cap if it misbehaved) is DEAD, do not reinstate. Common grounds. XT60 ~60A continuous (full-punch bursts may touch it; known).
VERIFY-BEFORE-WIRING (2026-07-27 sweep under CLAUDE.md truth rules 8+9; each = memory-based claim, damage-if-wrong; check datasheet/meter BEFORE connecting): (1) UBEC output: many hobby UBECs are 5V/6V jumper-selectable; 6V into UNO Q 5V pin may kill the board. Confirm jumper position + meter the output UNDER LOAD before first connect. UPDATE 2026-07-27: user metered 5.3V (no trim pot; presumed UNLOADED = floating-artifact suspect). Spec VERIFIED same docs page: 5V-pin window 4.5-5.5V max-not-exceeded -> 5.3V in-spec for the BOARD but only 0.2V ripple headroom, and same 5V_SYS bus back-drives camera VBUS (USB nominal max 5.25V, camera margin flagged not cleared). RULING: re-meter AT THE 5V PIN under real load (UNO Q + camera); <=~5.2V loaded = wire as planned; if it holds 5.3V, optional margin fix = 2nd adjustable buck (XY-3606-class) set to 5.00V by meter replacing the fixed UBEC feed. WIRING RULE (2026-07-27): UNO Q 5V feed must NOT be a dupont jumper (~1A class, I2R drop at 3A peaks -> droop -> reset; docs warn droop=resets and docs' >=3A = peak budget incl camera VBUS, not continuous draw). UBEC has NO output leads (user 2026-07-27; earlier "keep its own lead gauge" advice was an assistant form-factor assumption, retracted): user supplies ~18-20 AWG stranded, short run, soldered both ends (NOT servo-lead offcuts, 22AWG or thinner), MULTIPLE GND returns (per docs), and bench-measure actual loaded current (continuous draw unverified, expected well under 3A). Acceptance test stays: loaded voltage AT the 5V pin. UBEC output = SCREW TERMINALS (user 2026-07-27). Header-end ruling: NO bare stranded wire into female headers (poor contact geometry, stray-strand shorts to adjacent pins, vibration walk-out on THE critical rail). Preferred = Uno-R3 screw-terminal shield (UNO Q is R3 form factor; Robu.in/Amazon.in std part; VERIFY listing breaks out 5V/GND) -> both ends screwed. Fallback = wire soldered/crimped to male pin strip (2-3 pins grip better), hot-glue + zip-tie strain relief, 2x GND pins. Underside header-tail soldering only if board actually exposes tails (LOOK first, not asserted). (2) XY-3606: 5.00V setting (already ruled) AND output polarity metered before servo/ESP32 connect. (3) OLED SH1106 module: confirm the specific module's VCC range accepts 5V and its I2C lines are compatible with the Pixhawk 3.3V bus (module-dependent; check markings/listing). (4) TF-Luna: pinout per batch (already ruled), confirm 5V supply + 3.3V UART logic from the Benewake manual of THIS unit; pin5 float=UART. (5) ESP32 dev board 3V3 regulator: confirm headroom for TCA9548A + 7x VL53L0X before trusting the ring (measure current or check reg rating). (6) Power module: meter its 5V-ish FC output before first Pixhawk connect. (7) UNO Q 5V-pin feed: VERIFIED 2026-07-27 (docs: cover 5V load with 3A margin, short leads; VIN rejected because VIN power disables USB VBUS out = dead camera).

## PLATE LAYOUT (PROPOSED 2026-07-27, not yet built; VERIFY items = measure on frame, do not trust memory specs)
BELOW bottom plate (needs downward view / drop path): B525 nadir (offset so drop stream never crosses lens FOV); TF-Luna beside it clear nadir; hopper+MG90 at CG (payload mass stays centered as salt empties), outlet clear of camera. VERIFY leg ground clearance vs tallest underslung item incl squashed-landing margin.
BOTTOM plate / between plates (protected bay, monsoon): ESCs soldered to PDB pads + zip-tied arms (settled); UNO Q central (wiring hub: USB camera via hub, D0/D1 split cable, UBEC 5V; servo signal NO LONGER from UNO Q, see POWER MAP); ESP32+TCA9548A; 6x VL53L0X ring at plate edge 60deg (VERIFY 25deg FOV clears legs/props powered-on; decides ring height); UBEC+XY-3606 away from baro/FC; RX+SiK antennas clear of carbon/power; power module inline at battery lead.
TOP plate: Pixhawk on foam at center/CG arrow fwd baro covered (settled); GPS+compass mast far end from power wiring/ESCs; OLED glanceable (settled); buzzer outward; 7th VL53L0X up-facing at a corner (or bay edge w/ sky view, VERIFY).
BATTERY = open CG decision, decide by TAPE MEASURE (heaviest item): (a) central cavity between plates if 8000mAh pack fits = best CG/protected (then UNO Q/ESP32 move outward), else (b) strapped centered on top plate, Pixhawk shifts slightly (stays foam, near center). Plate gap NOT known from memory; measure.

## SERIAL MAP (final; 4/5 SWAPPED at build 2026-07-27: user wired Luna to the #4 lines, params updated to match instead of re-soldering; upside = SERIAL5 boot noise now hits junk-tolerant pymavlink not the Luna)
SERIAL1=SiK. SERIAL2(TELEM2)=ESP32 MAVLink2 115200. SERIAL3=GPS. SERIAL4=TF-Luna serial rangefinder 115200. SERIAL5=UNO Q MAVLink2 115200. SWAPPED SERIAL4/5_PROTOCOL in pixhawk_full_setup.param NOT YET PUSHED to board (push_params.py when Pixhawk next on USB).
SERIAL4/5 split cable (one 6pin DF13): pin1 5V->TF-Luna only (UNO Q powered by UBEC, never this pin); pin2 TX4->TFLuna RXD; pin3 RX4<-TFLuna TXD; pin4 TX5->UNOQ D0(RX); pin5 RX5<-UNOQ D1(TX); pin6 GND->both. TF-Luna pin5 float=UART (verify pinout per batch); its UART is 3.3V logic.
UNO Q D0/D1 = STM32 USART1 (PB7/PB6, 3.3V). Linux<->STM32 via Bridge RPC; plan = STM32 byte-shovel sketch, pymavlink unchanged on Linux; measure latency in SITL.

## PIXHAWK CONFIG (done state)
All 55 chosen params PUSHED+VERIFIED on board (tools/push_params.py, pymavlink per-write ack). Board exposes 1054 params; full backup pixhawk_every_param.param (regen: tools/pull_params.py, QGC closed). ALL param files live in param_dumps/ (user-created 2026-07-25); tools updated to that path; gitignore: param_dumps/* except pixhawk_full_setup.param (tracked = hand-maintained config; dumps regenerable).
TOOLING RULES: NEVER trust QGC bulk load (silently drops writes; also only reads its own tab format). Writes = push_params.py. Dumps = pull_params.py or QGC save. make_complete_params.py builds QGC-format complete file from a QGC dump (float32-aware).
4.7 renames encountered: ARMING_CHECK->ARMING_SKIPCHK (0=all checks); RTL_ALT->RTL_ALT_M (m); RTL_ALT_FINAL->RTL_ALT_FINAL_M; RNGFND1_GNDCLEAR->RNGFND1_GNDCLR (m, set 0.13 placeholder, MEASURE on airframe).
Notch: first hovers throttle mode (loaded: INS_HNTCH_REF from learned MOT_THST_HOVER, FREQ/BW from log FFT, INS_LOG_BAT_MASK=1 active); then adopt FFT mode (FFT_ENABLE 1, FFT_MINHZ/MAXHZ ~50/200, INS_HNTCH_MODE 4, REF 1; commented block in setup file; HAL_GYROFFT_ENABLED confirmed in build). "Chan 13 to 14 PWM failed no DMA" boot msg = expected, harmless.
Failsafes on board: FS_THR_ENABLE 1 (RTL), BATT low RTL / crit Land, ARMING_SKIPCHK 0. FENCE: deferred by user (commented block ready).
PRX gotcha: PRX1_TYPE=2 makes ESP32 an ARMING DEPENDENCY. Field recovery: PRX1_TYPE=0 + reboot.

## ESP32 OBSTACLE MODULE (esp32_obstacle_avoidance/)
7x VL53L0X via TCA9548A 0x70: ch0-5 ring 60deg (OBSTACLE_DISTANCE angle_offset 30), ch6 up (DISTANCE_SENSOR orient 24). 10Hz + heartbeat, sysid1 compid195. GPIO21/22 I2C; GPIO17->PixRX, GPIO16<-PixTX. Compiles clean both modes (MAVLink@2.0.31 okalachev, VL53L0X@1.3.1 pololu; pin versions). NEVER FLASHED yet.
Fake-mode flight guard: USE_FAKE_SENSORS=1 transmits only with GPIO4 jumpered to GND; else TX 0 B + warning. Preflight: banner must say REAL.
Array = reinstated PoC (never call rejected; don't dwell sunlight). Mount check: 25deg FOV must not see legs/props.

## CRASH LESSONS (permanent)
C1 prop nut backed off -> vibration -> alt corrupt -> prop departed (fix: handedness+Loctite). C2 AltHold wind drift into tree. C3 armed seconds post-power-on HDOP 65-99, vibration broke EKF, phantom-fall climb to 47m, pilot disarmed DELIBERATELY (drift to unrecoverable area); procedural, GPS healthy.
Rules: abort=flip Stabilize NOT disarm (disarm only imminent person-strike/unreachable flyaway). GPS modes: 10+ sats, HDOP<1.5, no EKF complaints, wait 2-5min. VibeZ median<15 hover gate before AltHold/Loiter. Insulate bare connectors; smoke stopper on repaired wiring first power-up.
VIBRATION = THE unsolved blocker (was median 20.6, peaks 60+; gate <15). Rebuild countermeasures: good props only, rigid motors, service loops, FC foam near CG, baro covered, GPS cable tied, notch, compass recal.

## ML / TRAINING
Pipeline (training/): merge_datasets.py (single class "puddle"; single-class sets kept whole; multi-class filtered to puddle/water-named; dropped boxes -> negatives; run with .venv python) -> train.py (yolo26n 640 batch8, ckpt/epoch, Ctrl+C pause, auto-resume last.pt; run1 v8n weights kept at runs/puddle_v1/weights/best.pt as fallback) -> export.py (ONNX for UNO Q onnxruntime).
Dataset v1 = 11725 train / 3069 val from: mosquito v1 (5069, puddle class only, CC BY 4.0, CITE BibTeX from its Universe page), puddle_Detect 4.93k, hanyang puddle-detection 1.5k + puddle 1.5k, water 1k, yinjia-part2. mosquito v4 EXCLUDED (no puddle class = poison; in training/excluded/). DATASET V2 QUEUE (researched 2026-07-25, all CC BY 4.0): PRIMARY = user nadir phone photos + drone stills + close-range frame-filling water (targets known failures); PLUS (user downloading 2026-07-25 night) thesis mosquito-breeding-grounds-2 ONLY (universe.roboflow.com/thesis-kjmym/mosquito-breeding-grounds-2, 3.47k, 7 classes incl Temporary Water Sites; CORRECTION: v3/v4/v5 spin-offs are container-only, NO water class = poison, do NOT use; never multiple versions of one pool = train/val leakage) + fumigation habitats (1.06k) + habitats2 (1.29k) (puddle + probable-stagnant-water-waist classes, ground-robot view). SKIP (researched): 82myj (112 tiny), Eds breeding-detection (556 container-heavy), Fumigation vol3/4/5 (no puddle class), all insect close-up sets, aedes/MBG unresearched-but-superseded. Verdict: NO retrain until v2 assembled (retrain ~7h GPU; these sets do not fix sheet-water/glare failures). merge KEEP_NAMES already extended for v2 class names; "pond" deliberately NOT kept (permanent water, not a drop target).
RUN1 (2026-07-25, ~e160/200): P 0.79 R 0.72 mAP50 0.789 mAP50-95 0.474, plateaued (v1 val set, NOT comparable to run2 numbers).
RUN2 COMPLETE 2026-07-28 20:00 (yolo26n, dataset v2 21717/4452, batch16, epochs cap 600): EarlyStopping @e322 (50-epoch plateau), BEST @e272: P 0.801 R 0.705 mAP50 0.766 mAP50-95 0.467 (v2 val). best.pt/last.pt in training/runs/puddle/weights/. VAL_COMPARE 2026-07-28 (user-run, both on v2 val): run1_v8n P .744 R .687 mAP50 .725 mAP50-95 .431 vs run2_26n P .795 R .708 mAP50 .766 mAP50-95 .467 -> RUN2 WINS ALL METRICS w/ fewer params (2.4M vs 3.0M) + 5.2 vs 8.1 GFLOPs; run1's higher v1-era numbers confirmed easier-benchmark artifact (v8n dropped .789->.725 mAP50 on v2 val). run2 best.pt = flight model. NOTE for docs/03: v2 has detect-segment mixed labels (3001 segments vs 3881 boxes on val; ultralytics uses boxes, drops polygons; harmless, disclose in dataset docs). NEXT (user-run): export.py -> scp ONNX to UNO Q -> fps benchmark + spot-check (TODO 1). V2-RETRAIN PLAN: yolo26n (adopted 2026-07-25, supersedes yolo11n pick; v12+ attention models too slow on A53 CPU), batch 16-24 (run1 used only ~1GB of 4GB VRAM), consider 416px export for ~2x UNO Q speedup; n-vs-s decided by ONNX benchmark on board (s runs but est <1fps; mission is stills-while-hovering so ~1s/frame acceptable).
Camera: B525 720p UVC; strip housing, weigh; lock focus v4l2-ctl; route USB away from GPS mast; test sat count cam-on vs off.
TF-Luna-over-water risk: 850nm IR vs still water = dropout/specular; dropout policy = abort UPWARD, never continue. Fallback (user-approved): hover+descend NEXT TO puddle.

## TODO (live; ordered by dependency; REWRITTEN 2026-08-02 at user request: finished items moved to DONE ARCHIVE below with whys intact. Old->new numbers where referenced elsewhere: old 12 -> 7, old 15-19 -> 13-17.)
BENCH (no flight):
1. FOV MEASUREMENT (calibration saga history in archive 9b): coin method at FLIGHT CONFIG (720p MJPG, focus locked 0 = sharpest per 2026-08-02 sweep), coins slid until half-cut by each frame edge, tape read on the floor, bright light. AWAITING user's D + W -> calibrate_fov(D, W) -> record HFOV here + docs/01, pass --hfov-deg on every run_mission invocation. Camera index MOVES between boots (video1 then video2); check v4l2-ctl --list-devices each session; TODO-small: switch detector/worker to the stable /dev/v4l/by-id path.
2. WORKER-SPLIT VERIFICATION (detect_worker.py + FileDetector written 2026-08-01, UNRUN): laptop py_compile uno_q/*.py; re-scp uno_q/ to board (also unblocks archive 10-VERIFY v and dashboard v2/v3 deploy); board smoke test = detect_worker.py --camera <current index> and watch /tmp/monsoonready_det.json seq advance. NO board dry-run possible yet (no Pixhawk tty, see 7; the earlier tcp:127.0.0.1:5760 board suggestion was wrong, nothing listens there).
3. Hopper build + salt flow test; watch bridging/clumping (Bti granules differ; monsoon humidity). Same bench: WEIGH the MG90 (metal gear heavier than SG90, ~650g payload budget), MEASURE its stall current vs the XY-3606/UBEC rating, and granule flow rate g/s (feeds area-dosing option c, variable dwell).
4. ESP32: flash fake-mode + GPIO4 jumper, verify serial lines; FAKE_VARY_CHANNEL sweep incl ch6.
5. Boot-noise test on SERIAL4 (Luna's port after the 2026-07-27 swap): ESP32 EN->GND as USB-serial; Pixhawk TX4->ESP32 TX0; watch 57600+115200 across power cycles. If noisy -> TF-Luna to I2C fallback (addr 0x10, pin5 low, Benewake I2C type, verify wiki). ALSO push swapped SERIAL4/5_PROTOCOL params (setup file already edited) when Pixhawk next on USB.
6. TF-Luna bench over water basin, indoor+outdoor, nadir+angle -> decides descend-over vs descend-beside -> if beside, set the lateral_offset_n/e_m knobs (exist in MissionConfig, default 0).
7. PIXHAWK-UNO Q LINK (was 12; BLOCKED Raghav 2026-07-31, plan may need rework): UNO Q docs CONFLICT on whether D0/D1 (=STM32 USART1 = `Serial1`) is usable for the Pixhawk link. User manual says D0/D1 = Serial1 on the JDIGITAL header AND ALSO says the router claims Serial1, do not open it in your own code; Arduino_RouterBridge README says the Bridge uses a core-routed UART "falling back to Serial1 if the core does not provide it". NOT RESOLVABLE FROM DOCS -> must be tested on the board. If Serial1 is claimed, the SERIAL5-to-D0/D1 wiring plan is DEAD and the fallback is a USB-serial adapter on the splitter/hub (already present for the camera). Byte-shovel sketch NOT written pending this. Until resolved there is NO Linux tty for the Pixhawk (run_mission --conn has no default for exactly this reason). Downstream once resolved: byte-shovel sketch OR dongle, serial soak vs SITL tcp, board dry-run.
8. ESP32->TELEM2 wire-up: OBSTACLE_DISTANCE+DISTANCE_SENSOR ~10Hz from comp195 in QGC MAVLink Inspector; then real sensors (USE_FAKE_SENSORS 0), hand-wave each direction.
9. USER INPUTS OWED: (a) D + W for item 1; (b) servo AUX channel confirm -> stage SERVO9_FUNCTION=0 + MIN/MAX in pixhawk_full_setup.param + push (until pushed, DO_SET_SERVO is silently ignored); (c) spare RTL switch channel -> RC6_OPTION,4 push; (d) VectoBac G sourcing (photo suffices for docs); (e) Simple Mode reconfig or not; (f) AREA-DOSING RESOLUTION (3 options in decision log 2026-08-01) - blocks the model/input-size adoption AND any dosing claim in docs or to a judge.
10. Dataset v2 capture: phone nadir photos from height + negatives (shadows/tarps/wet ground/rooftops/plastic) -> retrain -> export -> spotcheck -> THEN model/input-size decision by the recorded rule (misses on small/far puddles -> resolution n@736/960; sloppy boxes on well-seen puddles -> capacity s@640). Measured TOTAL menu (board): n@640 511ms / n@736 716 / n@960 1208 / n@1088 1529 (= s@640 1518) / n@1280 2126; m OUT. n@640 stays flight model until decided.
11. SITL DROPOUT DRILL (the one unfinished 10-VERIFY gate): expect ABORT_CLIMB, 0 drops, survey completes. Also SITL-check the worker-split runner path once a fake det file feeder exists or via --inline-detector.
12. Base station board deploy: cloudflared reinstall (/etc/cloudflared token wiped by reflash) -> verify drone.reysen.net; bg-run cmd = setsid nohup ~/venv/bin/python ~/uno_q/basestation/app.py > ~/basestation.log 2>&1 &
ASSEMBLY (in progress) then CALIBRATIONS:
13. Finish frame: ESCs soldered to PCB plate + zip-tied, motors rigid, FC foam, GPS mast, service loops, power module install + V/I calibration vs multimeter, XY-3606 to 5.00V, SERIAL4/5 split cable build, OLED splices soldered, buzzer, TF-Luna + camera mounts (weigh stripped B525), ESP32+mux+7 sensors (FOV check) incl 3V3/GND perfboard distribution hub next to mux (1 feed pair from ESP32 3V3 pin -> two bus rails -> 8 tap pairs; NEVER 8 wires on one pin; per-sensor 4-wire bundles VIN/GND/SDn/SCn, soldered+heatshrink, service loops; optional 10-100uF across rails; feed wire = bench current measure point), hopper mount.
14. Calibrations: accel+level, compass, radio, ESC cal, motor order/direction test, RC failsafe bench test (TX off, props off), battery calibration. Re-dump params after (pull_params.py).
15. Conformal coat LAST (mask baro/USB/connectors).
FLIGHT GATES (strict order):
16. Bench GPS check -> unloaded hover VibeZ<15 (THE gate) -> notch FFT switch + log verify -> Loiter -> loaded hover w/ ballast (relearn MOT_THST_HOVER, VibeZ recheck) -> payload integration -> mission flights -> demo footage (freeze ~Aug 10).
DOCUMENTATION (judged category; DRAFTED 2026-07-26 in docs/ 01-08 + index, see decision log):
17. REMAINING = the TBD values only (docs are reference prose in the esp32_obstacle_avoidance/README.md house style: numbered sections, tables, no task-list voice; work items live HERE not in docs): (a) dataset URLs + mosquito BibTeX, CC BY 4.0 licence obligation, REYANSH has the Universe pages [docs/03]; (b) v2/yolo26n metrics + UNO Q ONNX fps once trained/benchmarked [docs/01,06]; (c) Raghav: compliance verification pass vs CURRENT DGCA text (assistant cutoff Jan 2026, rules change; low confidence on aerial-larvicide route flagged in-doc) + true test-site description + Bti registration check [docs/05]; (d) "hardest part" personal answers [docs/06]; (e) ALL evidence capture, prioritised list in docs/07 (top 5: unoq_detection, full_loop, vibration_log incl FAILING one, sitl_dropout_drill, spotcheck_grid); (f) shoot video vs docs/04 storyboard (has Plan A auto / Plan B descope columns), footage freeze ~Aug 10.
NOTE (do not edit Build Log.txt, user-maintained): it still lists FC as "ArduCopter 4.6.3"; board is 4.7.0 since 2026-07-25. Docs use 4.7.0. ALSO (2026-08-01) two SG90 mentions remain in it (lines ~37 dispenser, ~40 parts ordered); every other SG90 in the repo is now MG90. Both are the user's to correct, flagged not edited. SUGGESTED BUILD-LOG ADDITIONS given to the user 2026-08-01 (his call, do not write them): FC firmware line 4.6.3 -> 4.7.0; SG90 -> MG90 swap + why; ETZIN EPL-1207CC OTG splitter as a REQUIRED part (the bare USB-C port cannot host a camera on any image, Type-C sink-only, case closed 2026-07-28); board reflash to the May-2026 image (wiped venv/data/cloudflared/keys); the SERIAL4/5 wiring swap. GIT NOTE 2026-08-01: user's uncommitted "Build Log.txt" edits blocked `git pull --rebase` ("cannot pull with rebase: You have unstaged changes"); it is a tracked user-maintained file so the fix is for HIM to commit it, never for the assistant to stash/discard it.
DESCOPE LADDER if time runs out: (a) Loiter + onboard detect + drop = minimum judgeable; (b) +base station report; (c) +obstacle array PoC; (d) +guided auto descent.

## DONE ARCHIVE (moved out of TODO 2026-08-02 at user request; relocation only, nothing deleted, whys intact; DECISION LOG holds more. Numbers below are the ORIGINAL TODO numbers.)
1. CLOSED 2026-07-29: run2 ONNX on board, benchmark 2.05fps A53 (489ms median), 24-img spot-check parity laptop-vs-board EXACT (0 count mismatches, conf delta .000, box delta 0.0px at recorded precision); grids both sides = docs/07 spotcheck_grid evidence (training/dataset/spotcheck_laptop/ + spotcheck_board/); onnxruntime /sys/class/drm probe warning on board = harmless, prints every run, ignore. Board also reachable on LAN 192.168.1.118 (DHCP, may change; tailnet IP in PRIVATE.md = stable address).
2. Camera on UNO Q: DONE 2026-07-29 (enumeration via ETZIN splitter on UBEC dumb 5V; 720p MJPG capture verified real JPEG; focus lock works as split transactions: focus_automatic_continuous=0 then focus_absolute=0). BOARD BENCHMARK DONE 2026-07-29 (user-run): median 489ms -> 2.05 fps on A53 CPU (2x headroom vs ~1s/frame mission bar; THE docs/01+06 number; onnxruntime /sys/class/drm GPU-probe warning = harmless, CPU EP intended). REMAINING: board/laptop parity spot-check (uno_q/spotcheck_onnx.py NEW 2026-07-29: 24-img set 14 pos/10 neg seed-42 from val -> training/dataset/spotcheck/, laptop baseline results in dataset/spotcheck_laptop/ incl grid.jpg = docs/07 spotcheck_grid material, 21 dets, frame-filling sheet-water miss visible as expected; user runs same script on board, scp results back, assistant diffs) -> full image->ONNX loop. UNO Q storage CORRECTION (user 2026-07-29): 32GB total (old "2.2G free" fact = pre-reflash image, stale). ONNX EXPORTED 2026-07-29 laptop: training/runs/puddle/weights/best.onnx 9.4MB, output (1,300,6) NMS-free end-to-end; IDENTITY VERIFIED yolo26n (metadata description + end2end shape + runs/puddle dir; user asked, confirmed NOT v8n).
9b. MODEL-SIZE MEASUREMENT (2026-08-01, user-run, replaces the assistant's guess): laptop `training/.venv/bin/python training/export_variants.py yolo26s yolo26m` -> training/variants/*.onnx -> scp to board -> `~/venv/bin/python ~/uno_q/bench_models.py --images '~/spotcheck/*' ~/uno_q/best.onnx ~/uno_q/yolo26s.onnx ~/uno_q/yolo26m.onnx`. DECIDE ON `TOTAL`, NOT FPS (single-threaded loop; TOTAL = how long poll() blocks the MAVLink pump). Reference point: yolo26n = 489ms median on this board. If a variant wins on TOTAL, it still costs ~12h GPU to train on v2 + re-export + re-spotcheck before it can fly, against an Aug 10 footage freeze. UNKNOWN: whether ultralytics publishes yolo26s/yolo26m under exactly those names (NOT asserted; the script passes the name straight through and ultralytics errors if it does not exist). Blocks the area/dosing decision above. 2026-08-01 session 2: bench_models.py letterbox re-verified byte-identical to detector.py; .gitignore now covers root *.pt (ultralytics downloads to CWD) + training/variants/ (30-100MB ONNX, repo goes public); board-side gotchas = bench_models.py must be scp'd (uno_q/ re-scp to board still pending per 10-VERIFY v) and the spotcheck image dir's exact path on the board is UNRECORDED, adjust --images if ~/spotcheck is wrong. MEASURED 2026-08-01 (user-run, board, 24 spotcheck imgs 640x640, ort 1.28 default threads): yolo26s 38.3MB TOTAL median 1921ms (min 1502 max 1999, 0.52fps, pre 19ms) = ~3.9x the n 489ms baseline; yolo26m 82.0MB TOTAL 4378ms (0.23fps) = OUT, no argument reaches 4.4s pump blocks. n baseline NOT re-run same-session: "MISSING ~/uno_q/best.onnx" = my path guess wrong (uno_q/ re-scp pending; where the 2026-07-29 benchmark's ONNX lives on the board is unrecorded, user to locate + rerun 3-way). VERDICT so far: s exceeds interval_s=1.0 (throttle no longer meaningful; pilot-override latency ~2s during SURVEY polls; coverage still fine, ~2s/frame supports ~3 m/s survey at 15m/60deg) -> s is MARGINAL, adoptable ONLY IF area-based dosing is adopted (its box-quality argument) AND ~12h v2 retrain fits before Aug 10 freeze AND interval_s raised to ~2s+; else n stays. m rejected on measurement. FINAL 3-WAY (user-run 2026-08-01, same session, same 24 imgs): n=511ms TOTAL, s=1518ms (2.97x), m=4374ms (8.56x). NOTE s measured 1921ms in its first run and 1518ms in the second, ~20% run-to-run variance (thermal/load), so decide with margin, not on the best run. VERDICT REVISED after the worker split (see decision log): s's latency no longer blocks the MAVLink pump, so its remaining costs are detection cadence ~1.5-2s/frame (caps survey speed for gap-free coverage) and ~12h GPU retrain + re-export + re-spotcheck before the Aug 10 freeze. m still out (nothing buys back 4.4s/frame cadence + 82MB). Adopt s ONLY with the area-dosing decision; n needs no change either way. BOARD ONNX PATH RECORDED: flight model = /home/arduino/best.onnx (NOT ~/uno_q/; found by user 2026-08-01). FOV CAPTURE ATTEMPT 1 FAILED (2026-08-01): /dev/video1 would not open and its focus controls are "unknown control" -> the B525 is NOT at video1 right now (unplugged or re-enumerated); camera index is UNVERIFIED until v4l2-ctl --list-devices is run. Board ~/uno_q is STALE (no run_mission.py), re-scp still pending. B525 IS AT /dev/video2 TODAY (2026-08-01, v4l2-ctl --list-devices; video3 = its metadata node, not capturable). The index MOVES between sessions (was video1 on 2026-07-29), so check --list-devices each session and pass --camera accordingly; detector.py's camera=1 default is unreliable. TODO-small: switch to the stable /dev/v4l/by-id/ path (cv2.VideoCapture accepts a device path string) so enumeration order stops mattering. FOV attempt 2 (2026-08-01): captured but UNREADABLE (handheld motion blur + dim warm light; tape ticks are only ~px-scale at 1.7m). Method switched to MARKER ALIGNMENT: camera resting stable, two bold objects nudged until they just touch the left/right image edges, then floor-measure between them = W; tape legibility in-photo no longer required. User suspects 640-fallback; capture command now prints f.shape to verify (must be (720,1280,3); if not, the geom preflight mismatch check exists for exactly this). USER ASKED 1080p + autofocus for the calibration shot: REJECTED, calibration must match the FLIGHT config (720p MJPG, focus locked infinity) because per-mode sensor crops and focus breathing can change the effective FOV (magnitudes unverified, which is exactly why the config must not differ); flying at 1080p itself rejected separately = zero model benefit (640 letterbox input) for more USB/CPU; speed cost ESTIMATED (not measured) at tens of ms/frame (decode+resize scale ~2.25x w/ pixels; inference unchanged, ~490ms n), so the argument against it is pointlessness, not speed; revisit only if tiling/cropping ever replaces downsampling. HIGH-RES MODEL QUESTION (user 2026-08-01: train n at 720/1080 input for long-range accuracy): assessment = at 15m/60deg a 1m puddle is ~37px in the 640 input, NOT tiny-object regime, and past recall failures were data-limited, so gains are likely modest, but MEASURE FIRST as usual: export_variants.py gained --imgsz (non-640 exports suffixed _<size>), bench_models.py now letterboxes to each model's OWN input size read from the ONNX. Bench n@1280 pretrained before any training decision; expected ~4x n@640 compute (ESTIMATE). Adoption consequences if it wins: detector.py hard-codes SIZE=640 (would need to read the model's input size), retrain at imgsz 1280 on 4GB VRAM = tiny batches/long run vs Aug 10, and it competes with s@640 for the same latency budget. Bench caveat: spotcheck imgs are 640x640 so the 1280 bench UPSCALES them; inference timing valid, pre timing approximate. MEASURED n@1280 (user, board, 2026-08-01): TOTAL 2111ms median (pre 80ms, 0.47fps, 10.4MB) = 4.32x n@640's same-session 488ms; the ~pixels-proportional scaling model holds, so interpolations are credible: 736=~650ms, 960=~1.1s, 1088=~1.4s (720/1080 are not valid YOLO input sizes, stride is 32, hence 736/960/1088). FULL LADDER MEASURED (user, board, 2026-08-01, warning-free after the fd fix): n@640 511ms / n@736 716ms (1.40x) / n@960 1208ms (2.36x) / n@1088 1529ms (2.99x) / n@1280 2126ms (4.16x); pixel-proportional scaling CONFIRMED (est. were 650/1100/1400/2100). KEY EQUIVALENCE: n@1088 costs the same as s@640 (1529 vs 1518ms) -> at the 1.5s budget the trade is resolution-on-n vs capacity-at-640, decidable only by training both (~12h GPU each). Cadence context: with the worker split nothing blocks; at 3 m/s survey, 2.1s/frame = ~6m spacing vs ~10m footprint depth (60deg placeholder), thin margin; 736 comfortable. DECISION RULE RECORDED: pick by v2 spotcheck failure mode (misses on small/far puddles -> resolution n@736/960; sloppy boxes on well-seen puddles -> capacity s@640); n@640 remains flight model until area-dosing is settled + v2 evidence exists. ORT WARNING ROOT CAUSE FOUND (why fix #1 failed, user asked repeatedly): ort 1.28 emits the /sys/class/drm warning AT IMPORT TIME, before set_default_logger_severity can exist; fixed properly 2026-08-01 with an fd-level stderr park around the import (detector.quiet_import_onnxruntime, same inline in bench_models.py). Board copies of bench_models.py/uno_q must be re-scp'd to pick it up. FOV attempt 3 (2026-08-01): D=0.66m, 1080p, STILL unreadable -> at that range the locked-infinity flight focus is genuinely defocused; resolution cannot fix defocus. Options recorded: marker method at D~1.5-2m where infinity focus sharpens (REJECTED by user: no way to fix the camera at 2m, handheld caused the attempt-2 blur), so the route is table-edge mount at 0.6-1m + AF, valid because FOV is distance-independent and sub-cm D/W errors at 0.66m are <1%; FOV attempt 4 (2026-08-01): paired inf/AF frames came back IDENTICAL per user (no sharpness change at all) -> either the blur is gain/denoise smear from dim light (AF cannot help) or the AF control never engaged and the lens never moved; the breathing check is INCONCLUSIVE until lens movement is proven. v4l2-ctl -l readout (2026-08-01): focus_absolute range 0-255 step 5 default 60, flags=INACTIVE while focus_automatic_continuous=1 (AF owns the lens, parked position unreadable); AF WAS on during the identical-frames pair, so it engaged and settled somewhere infinity-like. Next: manual focus SWEEP 0/60/120/180/255 at 1080p with per-frame Laplacian-variance sharpness printed (fs_*.jpg); SWEEP DONE (2026-08-02): lens MOVES (255 = mush) and focus 0 = SHARPEST for the floor at rig height (Laplacian 7.8 vs 6.9-7.2; low absolute values = noise-dominated). So AF had parked ~infinity (explains identical AF pair), flight focus is already optimal, limiter = dim-light noise/denoise. Breathing check moot (measurement happens at flight focus 0). FOV endgame: coin method AT FLIGHT CONFIG (720p, focus 0), coins slid until half-cut by each frame edge, tape read on the floor, more light. AWAITING D + W. Coin-on-tape fallback given (coins as edge markers, read tape on the floor, no image readability needed). AF allowed for the reading only IF focus breathing proven negligible by a paired capture (same unmoved scene, locked vs AF; identical edge extent = AF-read W valid for flight config). CALIBRATION AT 1080p ALLOWED with crop-equivalence check: capture same scene 720p AND 1080p camera unmoved; if scene extent matches, read W off the 1080p shot and it applies to the 720p flight config (verifies the per-mode-crop concern empirically instead of trusting memory). CORRECTION: the earlier "board dry-run --conn tcp:127.0.0.1:5760" suggestion was WRONG, nothing listens there on the board and no Pixhawk tty exists (TODO 12); until a link exists the board smoke test for the detector split is detect_worker.py alone + watching /tmp/monsoonready_det.json update. USER Q&A saved (why does inference "stop everything"): it is the single-thread ARCHITECTURE (deliberate: no locks, SITL/board identical), not hardware; during poll() the same thread that pumps MAVLink also does pilot-override/arrival/freshness checks, so those go blind for TOTAL ms; the AIRCRAFT keeps flying (position targets persist in ArduPilot) and DESCEND/DROP never poll the detector, so the cost is companion reaction latency (mainly STANDDOWN response), not a mid-air stall. Frame<->fix pairing stays consistent during the block (tel freezes with the frame since no step() runs). MITIGATION IF S ADOPTED: move inference to a worker process/thread with poll() collecting results = new concurrency code + re-test of the SITL-proven loop, cost it against Aug 10 before committing.
SOFTWARE (parallel; Mac=Raghav unless noted):
10. DONE 2026-08-01 (Reyansh/laptop, commit below): ALL review fixes (a)-(h) implemented + the MG90 rename + doc corrections; CODE IS UNRUN, see TODO 10-VERIFY. What changed, by file: mavlink_io.py = Telemetry gains vn/ve/vd_mps from GLOBAL_POSITION_INT (cm/s -> m/s, vd positive DOWN), velocity_ned(force=) bypasses SETPOINT_RESEND_S for setpoint CHANGES, PWM_MIN_US/PWM_MAX_US (800/2200) exported so construction-time and send-time checks agree, set_servo docstring marks the "does ArduPilot NACK when SERVOn_FUNCTION!=0" question UNVERIFIED. mission.py = new terminal state STOPPED (runner asked to wind up; unlike STANDDOWN it still commands end_mode, and unlike DONE it never shows an interrupted flight as a completed survey), should_stop= callback checked after the pilot-override test and before the state handler, DROP SPLIT IN TWO (DESCEND forces the zero setpoint then enters DROP; DROP holds, waits for |vd|<=settle_vd_mps 0.15 or settle_max_s 3.0, THEN opens the gate, then holds drop_dwell_s AFTER the gate closes; EKF floor still guarded during settle, ~1m of margin so it cannot false-trip at normal decel), _abort() now forces the climb setpoint on the abort tick, mission_end reports dropper.succeeded not .fired, basestation launch skipped if the port already answers + Popen OSError caught. missionlog.py = drop event gains ok (absent means true, for pre-2026-08-01 logs). dropper.py = MG90 everywhere, full servo-source rationale in the module docstring, pulse widths validated at CONSTRUCTION incl closed==open, construction close now RAISES (pre-arm proof; refuse to fly a dropper that has not answered) while trigger() still never raises and returns False instead, succeeded counter, gate-stuck-open warning; ServoDropper docstring no longer claims uno_q/sketch_bridge/ exists. detector.py = conf must be in (0,1] (conf<=0 used to accept the 300-row export's empty padding rows), preflight() grabs a frame before arming and fails on a dead camera or a frame-size/geom mismatch, mid-flight size mismatch falls back to nadir once + warns, ALL rows above conf are considered best-first (not argmax only) so a treated puddle no longer suppresses its untreated neighbour, _height_agl() prefers a fresh rangefinder return over rel_alt (above HOME) since ground_offset's contract is AGL. run_mission.py = SIGINT/SIGTERM/SIGHUP set a flag -> normal wind-up -> RTL; try/except BaseException -> _emergency_rtl (3 attempts, then tells the pilot to take the mode switch) + CRASHED:<state> mission_end; finally closes the log; --conn now REQUIRED (the /dev/ttyHS0 default was invented, no confirmed Pixhawk tty exists); MissionLog built AFTER the --dry-run return (no more phantom dashboard flights); dry-run says so when no GPS fix arrived; waypoint-file OSError = clean message; docstring documents setsid nohup as the PRIMARY ssh-drop defence. predict.py = unused defaultdict gone, missing/null t no longer becomes a 20000-day span (Site.stamp ignores None), _num() rejects bool-as-number, half_life<=0 and radius<=0 rejected, failed drops counted separately so a failed gate does NOT halve a site's revisit score, RECURRING/PERSISTENT thresholds named as constants. index.html = dashboard vocabulary now matches predict.py (its >=2-flight ring says "recurring", "persistent" is predict.py's stricter >=3 flights AND >=7 days), drops with ok:false draw as a red X ring and are excluded from the Treated count. camera_geom.py = mount_yaw sign now states its viewpoint. .gitignore = .DS_Store/Thumbs.db + the 3 committed blobs untracked.
10-VERIFY (user-run 2026-08-01, 4 of 5 PASS): (i) py_compile all of uno_q/ = COMPILE OK; (ii) test_camera_geom.py = 24/24 PASS; (iii-a) SITL HAPPY PATH = PASS with the new settle code, real transcript: "DESCEND -> DROP (rng=2.98m, stopping before release)" then TRIGGER #1 then "DROP -> CLIMB (treated)", drops=1, abort=None, final DONE, disarmed, identical rng to the pre-change run (2.98m) so the settle did not move the release height; (iii-b) DROPOUT DRILL = **STILL NOT RUN**, the one remaining regression gate (expect ABORT_CLIMB, 0 drops, survey completes); (iv) predict.py = PASS, 18 sites/12 flights, verdicts unchanged. NOTE scores drifted 1.68->1.67 and 0.60->0.59 between two runs of the same data: that is CORRECT, recency is computed against time.time() and the fake flights age in real time. Not a bug, do not "fix" it. (v) re-scp uno_q/ to the board STILL PENDING.
10-OLD (superseded, kept for the why): the original review findings were (a) run_mission.py wrap Mission.run() in try/except/finally + SIGINT/SIGTERM handler -> always mission_end + set_mode(RTL) (CRITICAL: bare run(), any exception/ssh-drop kills the loop mid-descent w/ no RTL, no end record); (b) drop-while-descending: mission.py zero-velocity setpoint before dropper.trigger() is silently eaten by mavlink_io SETPOINT_RESEND_S=0.2 rate limiter, so gate opens while still descending ~0.65-1m (worst ~1.5m on ACK loss) -> force-send the zero setpoint (bypass limiter) and/or verify vz~0 before trigger; (c) PixhawkServoDropper: validate closed_us/open_us at CONSTRUCTION vs the 800-2200 guard (bad open_us today = silent mission-long no-drop, in-range closed_us masks it), and do NOT swallow the construction-time initial-close failure -> a failed pre-arm close must refuse to fly, else phantom "treated" events land in JSONL/dashboard/predict; (d) SERVO<n>_FUNCTION=0 + MIN/MAX are docstring prose only, absent from param_dumps/pixhawk_full_setup.param -> stage them once channel confirmed (TODO 8), else push_params workflow never sends them and DO_SET_SERVO is rejected silently; (e) detector geom frame-size mismatch: OnnxDetector projects ACTUAL frame pixels through a CameraGeometry built from --frame-w/h, no equality check -> if MJPG 1280x720 negotiation fails and frames arrive 640x480, every offset is metres wrong, silently; add a check/rebuild; (f) rel_alt_m (above HOME) is fed to ground_offset(height_m) whose contract is AGL -> offsets scale wrong over non-level terrain (25% at 3m datum error); document + consider rangefinder AGL when valid; (g) detector considers only the argmax row, so an untreated puddle co-visible with an already-fired site is suppressed until the fired one leaves frame -> iterate rows above conf, dedup per row; (h) minor/nit: predict.py `t` default 0.0 (missing t = 20000-day span, `t: null` crashes) + half-life<=0 ZeroDivisionError + unused defaultdict import; run_mission MissionLog built BEFORE the --dry-run return (every bench dry-run leaves a phantom "in progress" flight on the judged dashboard); --dry-run silently no-ops indoors (poll returns None w/o GPS fix); no camera preflight (dead camera = whole survey flown, 0 detections); missing waypoint file = raw traceback; basestation auto-launch collides w/ an already-running instance; camera_geom mount_yaw "clockwise" lacks a viewpoint (belly-mounted = ambiguous sign).
11. UNO Q mission code REMAINING (core DONE 2026-07-26; OnnxDetector DONE 2026-07-29 laptop-tested 6/6: fire conf .77/throttle/same-site dedup/negative/new-site/no-fix; design: interval_s=1.0 throttle b/c single-threaded pump + ~0.5s board inference, skip_radius_m=8 fired-site memory prevents re-latch after CLIMB, conf=0.5 flight default, nadir assumption, frame_source injectable for tests, focus lock = 2 separate v4l2-ctl calls at init): pixel->ground offset DONE 2026-07-31 Raghav (camera_geom.py, 24 unit checks; HFOV STILL UNMEASURED = placeholder 60deg, runner omits geom unless --hfov-deg given, offset error is PROPORTIONAL to FOV error -> run calibrate_fov with a tape measure in final housing), dropper DONE as PixhawkServoDropper (see POWER MAP + TODO 10c/d), run_mission.py onboard runner DONE incl containment (TODO 10 fixes all applied 2026-08-01, UNVERIFIED until 10-VERIFY). REMAINING: descend-beside offsets once TF-Luna bench (TODO 6) decides, serial soak on real SERIAL5 vs SITL tcp (blocked by 12), board-side OnnxDetector live-camera test (user-run), 10-VERIFY.
13. Base-station: CODE DONE 2026-07-26; LAPTOP TEST PASS 2026-07-26 evening (assistant-run, see decision log: fake missions, API 404s/traversal, per-flight filter, layer toggles, tooltips, site-image, crash-truncation tolerance, 15s poll, dark/light CSS; 0 console errors). SITL END-TO-END PASS 2026-07-26 evening (laptop, both scenarios --record; dashboard renders the recorded JSONL; see decision log incl stats-before-render bug fixed + laptop xterm-launch gotcha). BOARD DEPLOY DONE 2026-07-29 (user: dashboard works from board over LAN). DASHBOARD v2 2026-07-29 (user requests, laptop-tested on 12 random flights, 0 console errors): wheel zoom (cursor-anchored, k 1..64, snap-home at 1) + drag pan (only when zoomed) + dblclick reset, implemented as affine view folded into proj.s/ox/oy post-fit so ALL layers scale free; abort tile red (--critical) mirroring treated green; gen_fake_mission.py --flights N --seed S random mode (serpentine surveys, shared site pool -> persistence, 75/25 treat/abort, one flight/day; no-flag default = original 2 curated flights). Flask dev-server warning = informational, acceptable for read-only single-viewer dashboard behind cloudflared (waitress = optional hardening, not needed for demo). KNOWN-COSMETIC: first paint differs from later paints by <0.1% px (font rasterization settling), self-corrects on any redraw. DASHBOARD v3 2026-07-30 (user bug reports): detections-toggle "did nothing" = REAL BUG, dots fully occluded (every det co-located w/ bigger drop/abort marker drawn later); fix = det layer drawn LAST w/ surface outline, verified px-exact (det blue vs drop green at same coord). Light theme REMOVED (dark-only, user request; matchMedia listener gone). Scale bar user-reported inaccurate but MEASURED CORRECT (ground-truth pair 30.64m: 5.292 px/m measured vs 5.286 proj.s, 0.1%, zoom-consistent); awaiting user's comparison method before touching it. REMAINING: user re-scp uno_q/ to board for v2 dashboard, cloudflared reinstall (token wiped by reflash) -> verify drone.reysen.net, onboard runner sets basestation_cmd (DONE Raghav 2026-07-31 in run_mission.py); bg-run cmd for board = setsid nohup ~/venv/bin/python ~/uno_q/basestation/app.py > ~/basestation.log 2>&1 &
14. DONE 2026-07-31 (Raghav): uno_q/predict.py recurrence scoring. Threshold reconciliation + nits DONE 2026-08-01 (predict.py holds the vocabulary: RECURRING >=2 flights, PERSISTENT >=3 flights AND >=7d span; the dashboard's >=2-flight ring now says "recurring" to match, so the map and the table can no longer label the same site differently). REMAINING: nothing, pending 10-VERIFY.
END OF DONE ARCHIVE.

## SESSION CONTINUITY (update every session end; laptop side current: 2026-07-25 evening; Mac side current: 2026-07-26)
MAC (Raghav): SITL env live (launch: ../ardupilot/Tools/autotest/sim_vehicle.py -v ArduCopter -f hexa --no-mavproxy --speedup 5 --add-param-file=uno_q/sitl_rangefinder.parm; PATH-prefix repo .venv/bin; it is a SERVER, runs until killed, never "completes"; do not pipe it to tail, that swallows output). uno_q/sitl_test.py PASS both scenarios 2026-07-26. Nothing LIVE on Mac as of 2026-07-26 (docs work finished+pushed).
MAC SSH TO BOARD: user=arduino confirmed (Raghav 2026-07-26), password auth today; tailnet IP not recorded (Tailscale installed on Mac but STOPPED; start it then `tailscale status | grep -i drone`). ed25519 keypair generated on Mac 2026-07-26 (~/.ssh/id_ed25519, NOT the laptop's key); pub key NOT yet in board authorized_keys -> Raghav runs ssh-copy-id (he types the password; password deliberately not stored in PRIVATE.md).
LIVE: (laptop/Reyansh, 2026-07-26) TODO 13 base-station code COMPLETE + pushed (untested; user will run tests himself, see TODO 13 REMAINING); Raghav do NOT touch basestation. User out of station: only laptop + UNO Q ssh reachable (no Pixhawk/camera/bench hardware). User runs commands/scripts himself this stint EXCEPT explicitly-delegated SITL setup (assistant-run, done). Laptop SITL BUILT 2026-07-26: /media/sleuther/Stuff/ardupilot-SITL @ tag Copter-4.7.0 (4.7 uses unified branch ArduPilot-4.7; per-vehicle 4.7.0 = tags on same commit), .venv inside (empy 3.3.4, pexpect, pymavlink), binary build/sitl/bin/arducopter; launch = Tools/autotest/sim_vehicle.py -v ArduCopter -f hexa --no-mavproxy --speedup 5 --add-param-file=<repo>/uno_q/sitl_rangefinder.parm (server, runs until killed). Cloudflared: user-installed ON BOARD (systemd-ish token /etc/cloudflared), drone.reysen.net -> localhost:8080, verified 502 (tunnel up, no server yet). run2 (yolo26n 21.7k) still NOT started.
LAPTOP SITL LAUNCH GOTCHA (2026-07-26): sim_vehicle.py on the laptop spawns arducopter inside an xterm (RiTW) which accepts TCP 5760 but never sends heartbeats (cause not diagnosed; single-threaded spin). SECOND GOTCHA (2026-08-01, assistant's error, cost the user 5 minutes): arducopter is a SERVER. "Waiting for connection ...." is its NORMAL idle state and it blocks there until a MAVLink client connects on 5760; it is NOT a hang and must NOT be Ctrl-C'd. It therefore needs its OWN TERMINAL, with sitl_test.py run from a SECOND one. Chaining them with && in a single shell can never work: the test only starts after arducopter exits, by which time port 5760 is gone (exactly the ConnectionRefused loop the user hit). Never hand out a one-terminal SITL command block again. WORKING laptop recipe: run the binary directly: cd /media/sleuther/Stuff/ardupilot-SITL && build/sitl/bin/arducopter --model hexa --speedup 5 --defaults "Tools/autotest/default_params/copter.parm,Tools/autotest/default_params/copter-hexa.parm,/media/sleuther/Stuff/Robu AI Challenge/uno_q/sitl_rangefinder.parm" -I0 (server, waits for first TCP connect, runs until killed; consecutive sitl_test.py runs work WITHOUT restart). If sim_vehicle route wanted, PATH-prefix .venv/bin or waf rebuild fails on missing empy. "Warning model expected 4 motors and got 6" at startup = harmless.
LIVE: laptop-side review of Raghav mission package PASSED 2026-07-26 (mission.py+mavlink_io.py verified vs PROJECT_STATE safety rules: latch, dual dropout-abort, EKF floor, STANDDOWN, compid 191, NED signs, orientation filter); run2 (yolo26n, 21.7k imgs) to be started by user tonight. RUN2 PREFLIGHT DONE 2026-07-26 night: dataset v2 verified merged (21717 train/4452 val, labels match), runs/puddle clear (no accidental resume; puddle_v1 = v8n fallback intact), yolo26n.pt local, GPU idle; train.py EPOCHS 200->600 (cap only; patience=50 plateau stop unchanged per user), BATCH 8->16 (per V2-RETRAIN PLAN, run1 used ~1GB/4GB; OOM fallback = back to 8). User launches detached (setsid nohup, log training/train.log). Pause/resume + pkill footgun: see run1 notes below. OOM INCIDENT 2026-07-27/28: kernel oom-killer killed run2 at e212 (22:24) and again on morning restart (09:19; journal-verified, laptop 27GB RAM but 13GB desktop use + 2GB swap 90% full; batch16 workers8 shm buffers = the overage; morning CUDA-OOM spam = dying instance still held VRAM). FIX pushed: train.py batch 8 + workers 8 (user keeps 8, will close other apps; workers ALSO passed on the resume path (ultralytics check_resume override list includes batch/workers, source-verified). Checkpoints intact, resume from e212 (metrics still climbing: e212 mAP50 .757 P .793 R .692). Overnight-run hygiene: close browsers/heavy apps (esp. the ~10GB of java seen in the OOM dump), ONE instance only (pgrep first), optional swap bump recommended. LAUNCH CMD now APPEND (user 2026-07-28): setsid nohup .venv/bin/python train.py >> train.log 2>&1 & (was >, which erased log history on every restart; train.py now prints a timestamped run header per start). 2026-07-27 morning check: e154/600, best P .763 R .686 mAP50 .741 mAP50-95 .450, fitness still improving (best=last, patience counter 0), ~4.8min/epoch, 12.4h cumulative; NOT comparable to run1 numbers (different val set: v2 4452 imgs, harder). NEW training/val_compare.py (user request): val-only pass scoring BOTH run1 v8n fallback + run2 26n on the v2 val split (same-benchmark comparison; needs GPU free). USER runs it manually after run2 finishes: training/.venv/bin/python training/val_compare.py (GPU must be free). RULE (user 2026-07-27, standing): NEVER create background services/watchers/scripts without asking first (an auto-run watcher was armed for this and removed on user request). STALE-BELOW: YOLO run1 notes kept for history: run1 training detached on laptop (setsid nohup .venv/bin/python train.py, log training/train.log, ckpts training/runs/puddle/weights/). Pause: kill the train.py process; FOOTGUN: pkill -f train.py inside a compound shell command matches the shell itself (exit 144, rest of command dies); use plain pgrep -f then kill PID, or run pkill alone. Resume: rerun train.py (auto-resumes last.pt). When done: nothing auto-fires; next = export.py -> scp ONNX to UNO Q -> benchmark.
AWAITING USER: (a) spare RTL 2-pos switch channel number -> then uncomment RC6_OPTION,4 + push_params; (b) B525 CONNECTED 2026-07-28 but TODO-2 BLOCKED: UNO Q USB-C never enumerates it. DIAGNOSED so far: typec port0 = sink-only, port_type/data_role sysfs read-only on this build; camera invisible even after community workaround (sudo echo host > /sys/kernel/debug/usb/4e00000.usb/mode, same fix as forum.arduino.cc/t/1412062; one user ran a dongle this way on VIN) + adbd stopped (it auto-restarts each boot; its dmesg spam = functionfs retry loop). SCHEMATIC-VERIFIED (ABX00162 pg20/22, PDFs in session scratchpad, redownloadable from docs.arduino.cc): JANALOG 5V pin IS net 5V_USB_VBUS = USB-C connector VBUS pins direct -> 5V-pin powering already puts bench 5V on camera VBUS (METER-CONFIRMED 2026-07-28: 5.20V at adapter A-socket VBUS, ~0.1V on GND side; ALSO = the UBEC loaded-voltage acceptance test from POWER MAP item (1): 5.20V loaded PASSES, 5.3V bench reading was the unloaded artifact, UBEC feed cleared); camera therefore POWERED, blocker narrowed to DATA path: host mode forced + xhci hubs up + powered camera = zero connect events (dwc3 debugfs toggle device->host verified working mechanically in dmesg: xhci teardown/re-register clean, gadget g1 -19 expected). Suspect = SoC USB2 PHY/typec mux not engaging host role on image BUILD_ID=20251210-442 (buildinfo checked; postdates the Nov-2025 "newer images fixed it" claim yet boots device-mode; workaround resets EVERY boot, adbd too). ISOLATION DONE 2026-07-28: keyboard+mouse also invisible (device-agnostic), /sys/kernel/debug/usb/devices = root hubs only, apt upgradable = nothing USB-relevant (alsa/atheros/qcom-soc fw only; apt-repo.arduino.cc present). WORKING THEORY (explains all evidence): powering via 5V pin = driving VBUS net = board permanently sees "charger attached" -> PMIC/typec holds PHY in device role regardless of dwc3 debugfs mode; matches staff "5V pin is output-only/unsupported input" + the one community success being VIN-powered (Sonoff+echo host). VIN TEST DONE 2026-07-28 (12V on VIN): VBUS at A-socket = 5.2V SELF-SOURCED (back-drive auto-on; "usb_vbus: disabling" @+33s does NOT kill it) -> OLD RULE "VIN disables USB VBUS out" METER-FALSIFIED. BUT keyboard+camera STILL zero enumeration on VIN + forced host -> blocker is NOT power and NOT dwc3 role: revised root cause = PMIC(PM4125)-typec HS data-path/mux never hands D+/D- to the SoC PHY while its port is sink-only (Qualcomm routes D+/D- through PMIC charger-detect); unreachable from userspace. "Newer image has the fix" claim DISCREDITED (fix repo's test = /etc/buildinfo merely EXISTS; ours exists, 20251210-442, still broken). WHY THE DOCK WORKS: PD hub speaks real PD to ANX7625 which resolves roles in hw (board = power sink + data host) = the Arduino-documented camera path. ADOPTED PLAN (2026-07-28): (1) USER buys USB-C hub w/ PD pass-through (Amazon.in; forum-proven iCAM 6-in-1 TC606 or equiv "PD pass-through" compact hub) = FLIGHT-VIABLE: UBEC 5V -> hub PD-in -> hub powers board via USB-C (supported path) + camera on hub A-port, ~20-40g stripped; bench VERIFYs on arrival: hub runs from dumb 5V on PD-in (some demand real PD negotiation; if so add PD-trigger board), forum plug order (board up first, then hub); (2) forum post w/ full diagnosis asking supported direct-host config (assistant drafts, user posts); (3) image update DEMOTED to release-notes-check-first (no blind flash). POWER MAP rewrite deferred until hub verified. sysrq HELP spam in dmesg = serial console noise, non-issue. SUPERSEDED SAME DAY: USER REFLASHED the board 2026-07-28 (his call, full wipe) to the May-2026 image: kernel 7.0.0-g122c2c22d838 (was 6.16.7), model string "Arduino UnoQ", HOSTNAME now arduino-drone, NEW TAILNET IP (PRIVATE.md updated; old IP dead). CONFIRMED on new image: dwc3 boots in HOST mode natively on VIN power (no debugfs echo, no adbd gadget spam) = the community "newer image" claim was real; usb_vbus still kernel-swept @+33s (cosmetic: unclaimed regulator auto-disable, hw gate self-enables on rails-good, meter-proven 5.2V post-disable on old image). FINAL DIAGNOSIS 2026-07-28 (case closed, new image): camera still silent; VBUS 4.7V self-sourced (buck+schottky+switch drops vs 5.2V bench-fed; above enumeration threshold); typec on new image = data_role [host], usb_role host, PD 3.0 stack, BUT power_role FIXED [sink] (no port_type knob) and NO port0-partner with camera plugged -> Rd(board-sink) vs Rd(passive adapter) = per Type-C spec NO ATTACH EVER, and without attach the PMIC never hands D+/D- to the PHY. Direct A-adapter camera on the bare port = IMPOSSIBLE on this board by design, on any image, any power source. Sonoff-on-VIN forum anecdote unexplained, abandoned. HUB = REQUIRED + CRITICAL PATH (gates all camera milestones, Aug 10 freeze): flight config = UBEC 5V -> hub PD-in (VERIFY dumb-5V accepted) -> hub upstream-C powers board as sink (SUPPORTED path; VIN wire and 5V-pin feed BOTH dropped from flight design) + B525 on hub USB-A. Buy: compact USB-C hub, explicit PD pass-through, >=1 USB-A, Amazon.in. INSIGHT: laptop "PD passthrough" hubs are electrically our exact case (hub sources on CC toward host when its PD-in is powered = the attach trigger; board sinks power + DR-swaps to data host) -> any well-reviewed laptop PD hub qualifies. CART DECIDED by user 2026-07-28: ONLY the ETZIN EPL-1207CC OTG splitter Rs660 (10-day returnable; that returnability is the load-bearing reason: Mport 52 REJECTED non-returnable, multi-item cart REJECTED; ~coin-flip risk accepted: 3.2/35 device-dependent reviews = passive-CC suspect, and board needs active source-advertisement on CC to attach; if it fails, next candidate must be RETURNABLE, filter hard on that). Delivery ~Jul 31. BENCH SEQUENCE on arrival (one variable at a time): (1) real PD phone charger -> splitter charging port, splitter C-plug -> board, ls /sys/class/typec: port0-partner = 2-minute pass/fail gate; (2) PASS -> camera on A socket, lsusb, full TODO-2 sequence, THEN flight-power experiments (UBEC dumb 5V on charging port; 12V-car-PD-adapter-on-3S idea parked as fallback); (3) FAIL -> same-day return + pick next candidate (returnable-only filter). SPLITTER ARRIVED EARLY + CAMERA GATE PASS 2026-07-29: lsusb on board (hostname arduino-drone) shows 046d:0836 Logitech B525 enumerated on Bus 001 -> ETZIN EPL-1207CC WORKS as attach trigger + data path, keep (returnability clause no longer needed); power source during pass = UBEC DUMB 5V (user 2026-07-29) -> FLIGHT-POWER GATE ALSO PASSED in the same test: UBEC 5V -> splitter charging port -> board powered as sink + camera enumerated = the full flight chain works, no PD-trigger board needed. CAMERA FACTS (new image, 2026-07-29): B525 = /dev/video1+video2 (video0 = Qualcomm Venus ENCODER, video3 = decoder; old video0 cmds stale); default negotiated fmt = raw YUYV 640x480 (stream-to dump = 614400B raw frame, NOT jpeg; must set MJPG 1280x720 explicitly); focus set-ctrl returns EACCES even under sudo (both ctrls in one transaction; split-and-retry + --list-ctrls diagnosis pending); scp = user runs it FROM the laptop pulling (laptop has no sshd; board-side push fails). PARALLEL while shipping: board re-setup per checklist above so ONNX benchmark runs day-of-arrival. BOARD RE-SETUP NEEDED (all wiped): ~/venv (python3 -m venv ~/venv; pip install onnxruntime opencv-python flask pymavlink), ~/monsoonready_data, cloudflared + /etc/cloudflared token (drone.reysen.net dead until then), laptop+Mac ssh keys re-authorized, v4l-utils, docker state, sudo password set by user. Raghav: board was flashed today, old IP/hostname invalid, re-run ssh-copy-id. phone-charge test INVALID for VBUS presence (C-C needs CC advertisement, sink-only port never advertises); back-drive Q2801 5V_SYS->USB_VBUS_IN gated by Q2802/LDO1_PG + SoC line VBUS_POWER_SWITCH_DISABLE_N (likely the sw usb_vbus regulator, 0 users, kernel disables @boot+33s). LEADS: (i) newer Arduino OS images enable host mode natively (forum + github Psalmustrack/arduino-uno-q-usb-fix README both say check /etc/buildinfo; that repo itself = just a systemd echo-host unit, NOT needed, and do not curl|bash it) -> USER to run: cat /etc/buildinfo; if old image, arduino-flasher-cli update = candidate clean fix BUT flash wipes board (~/venv, ~/monsoonready_data, /etc/cloudflared token, authorized_keys) AND needs Raghav coordination per multi-machine rule; (ii) mode-toggle device->host with camera attached (PHY may re-init only on transition), dmesg --follow while doing it; (iii) bench-only fallback = powered PD hub w/ plug order board-first (forum t/1430972), NOT flight-viable. NUANCE FLAG: recorded "VIN disables USB VBUS out" rule may be OS-image-dependent (newer images + Sonoff-on-VIN report contradict it for updated fw); re-verify after any image update; (c) VectoBac G sourcing; (d) Simple Mode wanted or not (was per-mode checkbox pre-reset, not restored).
PIXHAWK: on USB /dev/ttyACM0, config COMPLETE+verified, safe to disconnect. QGC may be open or closed; only matters for pymavlink tools (port exclusive).

## MICRO-FACTS (small but load-bearing)
UNO Q status LEDs (docs-verified 2026-07-28, user-manual): 4 RGB LEDs, 2 MPU-driven; status LED segments in /sys/class/leds = red:panic (the actual trouble light), green:wlan, blue:bt; solid blue=BT up, cyan-ish flashing=wlan activity on top of blue = NORMAL. Inspect: for f in /sys/class/leds/*/brightness; do echo "$f = $(cat $f)"; done. LEDs say nothing about supply quality; power acceptance = loaded meter at 5V pin.
Camera test cmds (UNO Q): lsusb | grep -i logitech; v4l2-ctl --list-devices; v4l2-ctl -d /dev/video0 --set-ctrl=focus_automatic_continuous=0 --set-ctrl=focus_absolute=0; capture: v4l2-ctl -d /dev/video0 --stream-mmap --stream-count=1 --stream-to=test.jpg.
UNO Q: Debian 13 (PEP668: pip only in ~/venv), sudo NEEDS password (never ask for it; give user the command), docker installed, /usr/lib/chromium 361M (do not remove, App Lab may need), ~ has ArduinoApps/, board hostname "drone".
Laptop training/.venv also carries: pymavlink 2.4.49, pyserial, yaml (via ultralytics), ultralytics 8.4.105, torch 2.13.0+cu130.
ESP32 build env: PlatformIO NOT globally installed on laptop; user has PlatformIO VSCode extension (their build path); Claude's route = python venv with platformio pip pkg (toolchains cached in ~/.platformio, survive). Build outside the repo or .pio is gitignored anyway.
Loaded-hover rule: if learned MOT_THST_HOVER > ~0.5 at full payload, trim payload (thrust margin thin; topic is user-settled, raise only via measured data).
Thrust math never measured on F550: treat loaded hover log as the authority.
gh CLI authed (account in PRIVATE.md). Git commits: NO Co-Authored-By trailer (user rule 2026-07-25). Commit authorship (2026-07-29): repo-local user.email = the GitHub noreply address (exact string + user's real email in PRIVATE.md ONLY; noreply chosen because repo will go PUBLIC for judges = harvest-proof, identical attribution; pre-2026-07-29 commits author sleuther@localhost, left as-is, history never rewritten). RULE: no real personal emails in any committed file.
QGC path + SSH key path: PRIVATE.md.
mosquito dataset page has BibTeX for citation; license CC BY 4.0 requires attribution in docs.
ESP32-as-USB-serial trick (for SERIAL5 sniff): ESP32 EN pin to GND holds chip in reset, its USB-UART bridge becomes passthrough; external TX -> board's TX0 pin -> PC.
3V3 sensor-rail budget VERIFIED 2026-07-27 from datasheets: TCA9548A Icc 20uA typ/35uA max (TI DS, 3.6V 400kHz); VL53L0X ~19mA typ ranging/~40mA peak/uA standby (ST DS); mux sequences so ~1 sensor ranges at a time; even impossible all-7-peak = 280mA. ESP32 dev-board 3V3 pin = onboard 5V->3.3V reg shared w/ ESP32 (WiFi unused): design fine on paper; VERIFY remaining = which regulator the actual board carries -> meter in series on 3V3 feed during 7-sensor bench test.
Old OneDrive project copy may still exist at /home/sleuther/OneDrive/Documents/Coding/ (folder was moved 2026-07-25; treat /media/sleuther/Stuff as only truth).

## DECISION LOG (append here)
2026-08-01 (Reyansh/laptop) RAGHAV'S SUMMARY TRIAGED (user forwarded it asking what still matters): every item is already recorded in state (camera_geom + site-dedup fix = TODO 11; predict.py = TODO 14; run_mission = TODO 13/10; decision 1 = TODO 12 blocked, needs the board test; decision 2 = servo on Pixhawk AUX, kept after 2026-08-01 re-litigation, waiting on the user's AUX-channel confirm + SERVO9_FUNCTION=0 staging per TODO 8/10d; decision 3 = FOV, measurement procedure handed to the user this session). One discrepancy: he wrote "SERIAL4-to-D0/D1", state says the plan was SERIAL5 then swapped, per the Build-Log-suggestions note; harmless either way since TODO 12 decides if ANY UART plan survives. His flagged easy fix (basestation summarize drops over-count) is now DONE, see next entry.
2026-08-01 (Reyansh/laptop) **DETECTOR MOVED OFF-THREAD (user-directed): detect_worker.py + FileDetector, in-loop inference kept as --inline-detector fallback.** The user's file-handoff design, implemented as specced because it is sound: uno_q/detect_worker.py owns camera+ONNX in its OWN process and atomically rewrites one JSON (tmp + os.replace; default /tmp/monsoonready_det.json, tmpfs so no SD wear) every cycle even with zero rows, so mtime doubles as the liveness heartbeat; detector.FileDetector reads it from the mission loop (stat per tick, JSON parse only on new seq). THE LOAD-BEARING SUBTLETY: a result read now is for a frame captured one inference ago (~5m of travel at 3 m/s with s), so FileDetector keeps a ~20s deque of telemetry snapshots and resolves the puddle against the snapshot nearest t_frame (wall clock, same machine), incl rangefinder freshness judged at frame time (at_t param). Refactor: shared row->Detection logic (geom-size fallback, best-first rows, site dedup, _locate/_height_agl) extracted to detector._RowResolver, used by both; OnnxDetector gains infer_rows() (grab+preprocess+run, no telemetry) which IS the worker's engine, so preprocessing cannot diverge. run_mission.py: spawns the worker (start_new_session, log ~/detect_worker.log, atexit terminate) or REUSES one whose file is fresh (<5s) = no double-spawn camera fight after a SIGKILLed runner; FileDetector.preflight waits <=30s for fresh output + frame-size check, camera_ok:false in the payload = camera dead vs file stale = worker dead. mission.py UNTOUCHED (poll interface preserved; SITL regression unaffected). Also fixed: ort.set_default_logger_severity(3) kills the /sys/class/drm warning (bench_models.py + OnnxDetector); basestation summarize() 'drops' now excludes ok:false (the flagged over-count). UNVERIFIED (no-commands rule), user must run: py_compile all of uno_q/; board dry-run of the split (worker spawn, preflight, staleness warn on kill -9 of worker, detection latch); set_default_logger_severity exists in ort 1.28 (API from model memory, VERIFY on first board run, symptom = AttributeError at startup); the 2026-07-29 laptop 6/6 OnnxDetector test predates this refactor and is STALE, re-run it plus a FileDetector variant (fake worker file) before flight. Consequence for model choice: with the pump no longer blocked, yolo26s's 1518ms stops being a safety argument and becomes only a cadence/coverage cost; interval_s stays as the worker --interval (default 1.0).
2026-08-01 (Reyansh/laptop) MODEL-SIZE VERDICT **REVERSED** by the user, and he is right. My "no" assumed the detection is only ever used as a CENTROID to fly to. User's actual plan: estimate puddle AREA from the detection box plus a height measurement, and size the Bti dose from it. Under that plan box QUALITY is load-bearing, not incidental, so mAP50/mAP50-95 buy something real and a bigger backbone can be worth its latency. New position: MEASURE FIRST, do not decide from either of our arguments. New files for that: training/export_variants.py (laptop; exports stock PRETRAINED yolo26s/m to ONNX at imgsz 640 purely for timing, COCO weights so their detections are meaningless, only the architecture/op-mix/shape transfer) + uno_q/bench_models.py (board; same byte-identical letterbox as detector.py, warmup then median/min/max, reports preprocess vs inference vs TOTAL and an x-slower ratio). The number that decides it is TOTAL, not fps, because mission.py is single-threaded and TOTAL is how long poll() blocks the MAVLink pump; compare it to detector.interval_s=1.0.
2026-08-01 (Reyansh/laptop) **AREA-BASED DOSING: DESIGN CONFLICT, NOT YET RESOLVED** (surfaced by the user's dosing plan; nothing implemented). Six problems, all load-bearing: (a) TF-Luna range is 0.2-8m but detection happens at SURVEY altitude (default 15m), where the Luna cannot read, so "area from the box + TF-Luna height" cannot work as stated at detection time; (b) mission.py DELIBERATELY does not re-detect during descent ("close-range frames unreliable, RUN1 spotcheck"), so there is no detection at any altitude the Luna can measure; (c) that leaves rel_alt (above HOME) as the only height available at detection, which is the AGL-datum error already flagged in TODO 10f; (d) a bounding BOX overestimates the water area of any non-rectangular puddle, and the error is unbounded (an L-shaped or diagonal puddle can be a fraction of its box); dataset v2 HAS polygons for ~3001 of 3881 val labels but ultralytics dropped them because we train a DETECT model, so segmentation is the honest route to area and is a different training run; (e) camera_geom.py has no area helper, only point offsets (ground_offset) and full-frame footprint; the correct area is not w_px*h_px*scale^2 because tangent-space scaling is nonlinear away from the image centre; (f) the dropper is a FIXED-dwell gate, so a variable dose means a variable dwell, which needs the granule flow rate from the TODO 3 bench test. RESOLUTION OPTIONS (pick before building anything): re-detect once at low altitude with a Luna-valid height (contradicts (b), needs a new decision + SITL work), or accept rel_alt-based area at survey altitude with the datum error documented, or descope to a fixed dose and report area as an ESTIMATE in the log/dashboard only. This must be settled before it is claimed in docs/01 or to a judge.
2026-08-01 (Reyansh/laptop) SERVO SILICON CORRECTION (user challenged with a datasheet, correctly): the STM32U585 DOES have hardware PWM timers, and nothing in this project's record ever claimed otherwise. The blocker was never the chip. It is (1) the Arduino UNO Q ships a ZEPHYR core and the stock Arduino Servo library does not build for it, so the pulse train needs an implementation written against Zephyr's PWM API + devicetree rather than Servo.h, and (2) the Linux side has to reach that sketch across the Bridge, which is TODO 12 and BLOCKED on the Serial1 contradiction. Plus the unverified 3.3V-into-MG90 logic threshold. Recorded because "the UNO Q cannot do servo PWM" would be a FALSE compressed version of this decision and would fail truth rule 8 on re-reading.
2026-08-01 (Reyansh/laptop) FULL-REPO READ (fresh session bootstrap, read-only, nothing executed beyond git per standing rule). One new MINOR finding: basestation/app.py summarize() reports drops = len(all 'drop' events) INCLUDING ok:false failed gates, inconsistent with the post-TODO-10 vocabulary where only actuated gates count as treatments (dashboard tile filters d.ok !== false; mission_end carries dropper.succeeded). Impact low: index.html never reads the summary's drops field (it counts from events), so only a direct /api/missions consumer over-counts. Fix when convenient = count ev['e']=='drop' and ev.get('ok', True) in summarize(). Everything else re-checked on this pass held up: DROP settle split ordering, _stopped() fallbacks, detector _fired-before-treatment dedup (intended: abort never retries a site, README 4.2), _at_wp alt access safe (GLOBAL_POSITION_INT sets lat+rel_alt together).
2026-08-01 (Reyansh/laptop) SERVO CHANGED SG90 -> MG90 (user: metal gears, better precision). Every SG90 mention in the repo renamed (README.md, docs/01, uno_q/dropper.py x3, PROJECT_STATE x2); Build Log.txt has 2 more but is user-maintained so it was FLAGGED not edited. Open VERIFYs recorded in POWER MAP: exact variant (MG90S vs MG90D), mass (metal gear is heavier, weigh it against the ~650g payload budget), stall current vs the XY-3606 (higher than SG90; no verified datasheet number, measure on the TODO 3 flow test).
2026-08-01 (Reyansh/laptop) SERVO SIGNAL SOURCE RE-EXAMINED at user request ("power it through the arduino directly, not the pixhawk unless there is a valid reason") and KEPT ON THE PIXHAWK, because there is one. Full four-step why is in POWER MAP and in uno_q/dropper.py's module docstring; the short version is that the Arduino route needs a Zephyr PWM implementation that does not exist plus the Bridge that TODO 12 is blocked on, while the Pixhawk route needs no new code at all, 14 days from the deadline. Separately, the user's stated premise ("the servo is powered by the external 5V module so the 3.3V GPIO should not be an issue") addresses POWER, which was never the objection: servo power has always come from the XY-3606 on BOTH routes. The objection is (a) the missing firmware and (b) LOGIC LEVEL, 3.3V GPIO into a servo whose input-high threshold no consulted datasheet states. Recorded so this is not re-litigated from a bare verdict (truth rule 8). Reversible: if the user still wants the Arduino route, the cost is the Zephyr PWM sketch + resolving TODO 12 + a scope check on the pulse.
2026-08-01 (Reyansh/laptop) ALL PEER-REVIEW FIXES IMPLEMENTED (TODO 10 (a)-(h), 1 critical + 4 major + the minors), file-by-file record in TODO 10 above. Three of them changed behaviour that SITL previously proved, so SITL is a BLOCKING regression gate before any of this flies: (1) DROP now settles before the gate opens, (2) the runner catches signals and exceptions and commands RTL, (3) a failed gate is recorded as ok:false and is no longer counted as a treatment anywhere (log, dashboard, predict score). NOTHING WAS RUN: per the standing 2026-08-01 rule the assistant executed no commands on the laptop or the board this session beyond git, so every one of these fixes is UNVERIFIED CODE. Do not treat TODO 10 as closed until 10-VERIFY passes.
2026-08-01 (Reyansh/laptop) MODEL SIZE QUESTION (user: is yolo26s/m worth it at 2 fps?): NO, and the reason is that fps is not the binding constraint, coverage is. At 15m survey alt with the 60deg placeholder FOV the along-track footprint is ~6.5m, so 2.05 fps supports up to ~13 m/s of survey speed, far above anything this aircraft will fly; the detector already throttles itself to 1 fps deliberately (interval_s=1.0) because the mission loop is single-threaded. The real limit on detection quality is recall 0.70 on frame-filling sheet water, which is a DATA failure mode (documented in the run2 spotcheck) that a bigger backbone does not fix. Cheaper wins in order: dataset v2 nadir/frame-filling images (TODO 9), then measuring HFOV, then int8 quantisation if speed ever does bind. IF the question is revisited, do NOT guess the fps cost: export a pretrained yolo26s to ONNX and benchmark it on the board first (no training needed for a speed number), then decide.
2026-08-01 (Reyansh/laptop) PEER REVIEW of da64b13 (Raghav's TODO 11/13/14 + servo decision), multi-agent: 6 subsystem readers + 6 review lenses -> 46 candidate findings -> 3-lens adversarial refutation (spend limit truncated the verify pass at 38 of ~138 votes; the rest re-derived by hand from the code). Verdict: the ARCHITECTURE is sound and mission.py/missionlog.py contracts are upheld; SITL-proven state machine untouched; predict.py + camera_geom.py maths check out (24 geometry checks are real identity tests, not tautologies; the vfov-tautology and letterbox-clamp findings were REFUTED 3/3). Confirmed defects -> TODO 10 (a-h) above: 1 CRITICAL (no exception/signal containment around Mission.run: an ssh drop or any raise kills the loop mid-descent, no mission_end, no RTL command; ArduPilot's guided setpoint timeout is the only backstop), 4 MAJOR (drop fires while still descending because the zero-velocity setpoint is eaten by the 0.2s setpoint rate limiter; servo pulse widths unvalidated at construction; construction-time initial-close failure swallowed so a dead dropper still flies and logs phantom "treated" events that also halve predict.py's revisit score; detector geometry uses configured frame size against actual frame pixels), plus doc/state contradictions (dropper.py module docstring still states the SUPERSEDED "STM32 side, settled" verdict and cites uno_q/sketch_bridge/ which does not exist; uno_q/README.md Files table still calls OnnxDetector/ServoDropper stubs, omits the 4 new files, and section 8 lists all 3 completed items as open while recommending the dead STM32 route; POWER MAP still said "Servo signal = UNO Q GPIO" -> FIXED in this commit per truth rule 8/9 since it is a wiring instruction). Also: 3 .DS_Store binaries committed and .gitignore has no rule. NOT reviewed by execution: nothing was run on the user's machine or the board this session (user restriction 2026-08-01); SITL/predict/test_camera_geom re-runs remain user-run.
2026-08-01 (Reyansh/laptop) RULE EXTENDED (user): assistant runs NO commands on the user's laptop or the UNO Q without explicit per-instance permission; git pull/push are pre-authorised. Supersedes the narrower 2026-07-29 board-only rule. Deliverable format: batch every command the user must run into one block at the end of the reply.
2026-07-27 (Reyansh/laptop) SERIAL4/5 roles swapped: user physically wired Luna to #4 lines / UNO Q to #5 lines during rebuild; decided to keep the wiring and swap SERIAL4_PROTOCOL(9)/SERIAL5_PROTOCOL(2) instead of re-soldering. Rationale: electrically identical (both 3.3V 115200), fewer joints, and SERIAL5's suspected boot noise now faces pymavlink (junk-tolerant parser) instead of the Luna. Setup file edited; push pending next USB session; TODO 5 boot-noise sniff rescoped to SERIAL4 TX.
2026-07-27 (Reyansh/laptop) VIN near-miss postmortem -> two standing rules added to CLAUDE.md truth rules: (8) decisions keep their load-bearing WHY in PROJECT_STATE, bare verdict = unverified, re-verify before acting, why is never dropped in compression; (9) hardware actions verify the governing spec from datasheet/docs/meter first, never model memory. Trigger: "5V pin NOT VIN" was recorded without rationale; VIN powering (7-24V, real spec) almost adopted; actual blocker (VIN power disables USB VBUS out -> camera dead; Arduino staff: unsupported) only caught because user challenged. POWER MAP gained a VERIFY-BEFORE-WIRING checklist (UBEC 5V/6V jumper, XY-3606 polarity, OLED VCC/I2C levels, TF-Luna batch pinout, ESP32 3V3 budget, power module output).
2026-07-26 (Reyansh/laptop) TODO 13 SITL end-to-end PASS: laptop SITL + sitl_test.py --record, happy path (1 drop rng 2.98m) + dropout drill (abort up, 0 drops) both PASS recording real-mission JSONL (32+28 lines, 0 bad); dashboard renders both flights, cross-flight persistent site = 1 (same puddle latched twice). BUG FOUND+FIXED in static/index.html: reload()/flight-change ran stats() before render(), but the persistent-sites tile needs the projection render() fits -> tile showed '-' for the first 15s after page load (masked in earlier fake-data test by the poll's second reload). Fix = reorder render() before stats(); verified <=1s now. Laptop sim_vehicle/xterm launch gotcha recorded in SESSION CONTINUITY (direct-binary recipe works).
2026-07-26 (Reyansh/laptop) TODO 13 laptop test PASS (assistant-run in in-app browser, scratch data dir, Flask from training/.venv 3.1.3): gen_fake_mission via real MissionLog incl injected-clock mission ids; /api/missions summaries exact; bad-id + %2f traversal + no-site-meta all 404; per-flight filter scopes stats/table (persistent tile '-' single-flight = by design); layer toggles verified by canvas pixel sampling; tooltips incl persistent-site ring text; site-image background (georef json + png) draws dimmed under data, checkbox auto-appears, escape attempt 404; truncated-last-line JSONL skipped, endless mission -> "in progress / interrupted"; page auto-picked-up 3rd flight via 15s poll; 0 console errors. KNOWN-NON-BUG: canvas doesn't repaint on EMULATED color-scheme flip (CDP emulation doesn't fire matchMedia change listener; listener code correct, real OS theme change fires it; CSS itself flips instantly). .gitignore += .claude/ (per-machine tool config).
2026-07-19 servo cap skipped; salvage only if servo misbehaves.
2026-07-20 handoff v3; ESP32 fw updated 6@60deg+up; sensors 7x plan.
2026-07-24 serial option B (split SERIAL4/5 cable) chosen over TF-Luna I2C; hexa X confirmed.
2026-07-25 project moved to /media/sleuther/Stuff/Robu AI Challenge; all parts arrived; frame assembly started; ESP32 powered from servo BEC (XY-3606); charger=HTRC B6 V2; fw compiles clean both modes; fake-mode GPIO4 TX guard added; pixhawk_full_setup.param created; local YOLO training adopted over Edge Impulse cloud; standing-water framing adopted; AVOID_MARGIN 1 / AVOID_DIST_MAX 1.5 sized to sensor range; failsafes confirmed; FENCE deferred.
2026-07-25 firmware upgraded to Pixhawk1-bdshot 4.7.0 stable; bdshot RPM notch found impossible for hexa (AUX5-6 NODMA); adopted in-flight FFT notch; motors stay MAIN.
2026-07-25 board factory-reset by user (no prereset backup; switches/modes reconstructed from screenshot+handoff). QGC bulk-load found to silently drop writes -> pymavlink push_params.py with per-write ack became the standard; all 55 params verified; RNGFND1_GNDCLR rename found. Full-param backup workflow live (pull_params.py).
2026-07-25 datasets: mosquito v4 excluded (no puddle class), v1 adopted (2.9k-image set, 5069 imgs incl splits, CC BY 4.0); merged 11725/3069; run1 started, restarted once to include mosquito (8min lost). Private git repo created (URL in PRIVATE.md), commit+push-every-change rule adopted; generated param files + datasets gitignored, pixhawk_full_setup.param tracked.
2026-07-25 PROJECT_STATE restructured: done-work pruned into this log, TODO section now reflects only remaining work.
2026-07-26 Mac bootstrap (Raghav): second machine online (darwin; NO training here, all training = Reyansh's RTX3050 laptop per Raghav); PRIVATE.md stub created from sample on Mac (values unfilled); stale yolo11n ref in V2-RETRAIN PLAN corrected to yolo26n.
2026-07-26 (Raghav/Mac) TODO 19 DRAFTED: docs/ set written from PROJECT_STATE + Build Log + code, 9 files: README index, 01 project writeup (problem/system/hardware/model/mission-safety/what-is-NOT-done incl vibration blocker stated openly), 02 crash postmortems (C1/C2/C3 root cause + bench PASS/FAIL table + the "2 of 3 = vibration corrupts alt estimate" through-line), 03 dataset citations (v1+v2 tables, rejected-sets table w/ reasons, leakage + no-pool-class methodology defences; URLs/BibTeX = FILL, CC BY 4.0 obligation), 04 video storyboard (12 shots w/ "what it proves" column, Plan A auto vs Plan B descope, capture checklist), 05 compliance (honest gap statement, confidence-tiered rule claims, cutoff warning, deployment roadmap; needs Raghav verification), 06 judge Q&A (~25 Qs incl the hostile ones: 3 crashes, 0.72 recall, do-you-meet-the-vibration-gate=NO, blind-descent subtlety), 07 evidence checklist (what to shoot + what each artefact proves + priority-5 if time short; says capture the FAILING vibration plot), 08 AI-authorship disclosure (incl where the assistant was WRONG: param renames, bdshot notch impossibility, the abort rule that would have aborted every descent). Rule applied throughout: FILL markers instead of invented URLs/metrics/reg claims.
2026-07-26 (Reyansh/laptop) TODO 13 base-station design settled: live web server on UNO Q (uno_q/basestation/); heatmap on blank schematic canvas (scale bar + north arrow) with OPTIONAL user-supplied georeferenced site image as background when present; data feed = mission.py appends per-mission JSONL event log (state changes, detections, drops, aborts, periodic fixes), base station reads files only; mission.py AUTO-LAUNCHES base station on DONE/STANDDOWN; ALL map layers (detections/treated/track/aborts/stats) checkbox-toggleable; accumulates site history ACROSS missions with per-flight filter (persistence framing). Open: stdlib-vs-Flask, cloudflared tunnel (explaining to user). Benchmarking run1 v8n ONNX on board REJECTED by user (pointless, yolo26n is the target).
2026-07-26 (Reyansh/laptop) TODO 13 BUILT: Flask adopted (user choice; one-time ~/venv pip install on board, offline after). Cloudflared installed by USER on the board -> https://drone.reysen.net (token /etc/cloudflared). New: uno_q/missionlog.py (JSONL schema single source; injectable clock), mission.py wired (recorder= optional param via _NoLog null-object, SITL tests untouched; events: start/fix@1Hz/state/detection/latch/drop/abort/end; cfg.basestation_cmd argv auto-launches server on DONE/STANDDOWN), uno_q/basestation/{app.py (read-only Flask: /api/missions,/api/events/<id|all>,/api/site_meta,/api/site_image), static/index.html (self-contained vanilla-JS dashboard: canvas heatmap w/ sequential blue ramp, layer checkboxes, persistent-site rings = detections <=5m across >=2 flights, per-flight filter + accumulation, stat tiles, tooltips, event table, scale bar/north arrow, light+dark, 15s poll), gen_fake_mission.py (2 fake flights via MissionLog w/ fake clock), README.md}. Site-image background = optional user-supplied north-up screenshot + corner-bounds site_image.json in data dir. Data dir ~/monsoonready_data (outside repo, nothing gitignored). Root README.md created (repo map); CLAUDE.md got basestation + laptop-SITL standing facts.
2026-07-26 (Raghav/Mac) Mac SSH: ed25519 keypair generated; UNO Q password NOT stored on disk (plaintext creds risk; project convention = user types passwords); PRIVATE.md filled except tailnet IP. ArduPilot Copter-4.7.0 SITL built on Mac (sibling shallow clone, matches board fw; RNGFND SIM type 100 verified in source). uno_q/ package created: mavlink_io.py (single-threaded pump, per-command ACK w/ retry incl FAILED-during-EKF-settle, compid 191, SET_MESSAGE_INTERVAL GLOBAL_POSITION_INT@5Hz + DISTANCE_SENSOR@10Hz), mission.py (state machine: TAKEOFF/SURVEY/APPROACH/DESCEND/DROP/CLIMB/ABORT_CLIMB/DONE/STANDDOWN; TARGET LATCHING at survey alt; dropout=abort-UP with acquired/expect-by-6m logic b/c TF-Luna blind above ~8m; EKF floor guard; pilot mode-flip => STANDDOWN never fight pilot), detector.py (FakeDetector; OnnxDetector stub), dropper.py (LogDropper; ServoDropper stub pending PWM source), sitl_test.py (scripted scenarios + param guard). SITL results: happy path PASS (1 drop @ rng 2.98m, survey resumes, RTL, disarm); dropout drill PASS (abort up, 0 drops, survey completes). sitl_rangefinder.parm mirrors TF-Luna 0.2-8m.
2026-07-26 (Raghav/Mac) docs/ REWRITTEN to house style after user rejected v1 as "instructions to me and Reyansh" not documentation. STYLE RULE now binding on all repo docs: match esp32_obstacle_avoidance/README.md = numbered sections + --- separators, tables for anything tabular, ASCII diagrams, real commands with real expected output, design-notes + troubleshooting sections, reference-prose voice. NO task-list voice, NO per-person assignments, NO status/progress columns inside docs; unknown values = bare "TBD" (was "FILL: <person> to do X"); remaining work lives in PROJECT_STATE TODO only. Same rewrite applied to uno_q/README.md (added state-machine ASCII diagram, MissionConfig table, abort-condition table, SITL transcripts, troubleshooting table). Content/facts unchanged, voice+format changed.
2026-07-26 (Raghav/Mac) NOTED laptop TODO-13 design dependency on MY file: base station wants mission.py to append per-mission JSONL events (state changes/detections/drops/aborts/periodic fixes) + auto-launch base station on DONE/STANDDOWN. NOT implemented by Mac (TODO 13 claimed by laptop, "Raghav do NOT start it"); whoever owns the hook must decide - flagging so it does not fall between machines.
2026-07-26 (Reyansh/laptop) Raghav's JSONL-hook flag RESOLVED: mission.py recorder hook + basestation_cmd auto-launch implemented laptop-side the same day (see TODO 13 BUILT entry above; rebase crossed his flag). uno_q/README.md conflict resolved keeping Raghav's house-style rewrite, missionlog/basestation rows added to its Files table; basestation README + new root README restyled to house style.
2026-07-31 (Raghav/Mac) TODO 11 pixel->ground DONE: uno_q/camera_geom.py (nadir pinhole; ground_offset scales in TANGENT space not pixels; camera_to_ned rotates by heading+mount_yaw; letterbox_to_frame undoes the detector's 640 letterbox; footprint_m for survey-leg spacing; calibrate_fov = tape-measure procedure). 24 unit checks PASS (uno_q/test_camera_geom.py). OnnxDetector now takes geom= + mount_yaw_deg=, extracts BEST ROW (was max-conf-only), locates box centre, and DEDUPS ON SITE not on drone position (same puddle from 2 positions = 1 target). Degrades to nadir if geom None or heading/rel_alt missing (never raises mid-mission). HFOV IS UNMEASURED: camera_geom.DEFAULT_HFOV_DEG=60 is a placeholder marked VERIFY; offset error is PROPORTIONAL to FOV error; runner omits geom unless --hfov-deg given.
2026-07-31 (Raghav/Mac) SERVO PWM SOURCE DECIDED = PIXHAWK, not UNO Q. Chain: Linux soft-PWM jittery -> planned STM32-via-Bridge -> RESEARCH FINDING: stock Arduino Servo lib does NOT support Zephyr cores (UNO Q is Zephyr), so STM32 route needs a board-specific PWM impl. Pixhawk already has hardware PWM outputs, already powered/wired/talking MAVLink to us; motors occupy MAIN1-6 so an AUX output is free; AUX5-6 NODMA blocks bidir-DShot ONLY, not ordinary PWM servo. => dropper.PixhawkServoDropper (MAV_CMD_DO_SET_SERVO via new MavIO.set_servo, 800-2200us guard, closes gate on construction in case a crashed run left it open, bridge/link failure logged NEVER raised into the state machine). Bridge-based ServoDropper kept as blocked alternative. WIRING CHANGE: servo SIGNAL now from a Pixhawk output pin, NOT UNO Q GPIO; servo POWER unchanged (XY-3606). PARAMS NEEDED: SERVO<n>_FUNCTION=0 (Disabled) or DO_SET_SERVO is ignored, + SERVO<n>_MIN/MAX bracketing the two pulse widths. CHANNEL NOT CHOSEN (default 9 = AUX1, user to confirm free).
2026-07-31 (Raghav/Mac) TODO 12 BLOCKED, plan may need rework: UNO Q docs CONFLICT on Serial1. User manual says D0/D1 = PB6/PB7 = USART1 = `Serial1` for the JDIGITAL header, AND ALSO says "The router claims the hardware serial port Serial1 ... Do not attempt to open Serial1 in your own code". Arduino_RouterBridge README: "Bridge object is defined over an UART port routed by the zephyr core, FALLING BACK TO Serial1 if the core does not provide it". => whether D0/D1 is usable for the Pixhawk depends on whether this core build gives the Bridge a dedicated UART (LPUART1/Serial2) or falls back to Serial1. NOT RESOLVABLE FROM DOCS, must be tested on the board. If Serial1 is claimed, the SERIAL4/5-to-D0/D1 wiring plan is dead and the fallback is a USB-serial adapter on the powered hub (hub already needed for the camera). Byte-shovel sketch NOT written pending this.
2026-07-31 (Raghav/Mac) TODO 14 DONE: uno_q/predict.py, recurrence scoring over the same JSONL the base station reads (read-only, does not touch basestation/). Deliberately NOT a rainfall model: scores score = flights_seen x recency(half-life 14d) x 0.5-if-treated, clusters detections/drops/aborts within 5m (running-mean centre), verdicts single/recurring/persistent (>=3 flights AND >=7d span). Every term defensible, nothing fitted, no train/test leak possible. Verified: 2 curated fake flights (finds the 1 persistent site, agrees with dashboard), 12 random flights seed 7 (top site 4 flights/7.8d), and a REAL SITL --record flight.
2026-07-31 (Raghav/Mac) TODO 13 Mac-side part DONE: uno_q/run_mission.py onboard runner (OnnxDetector+PixhawkServoDropper+MissionLog+basestation_cmd wired; --dry-run refuses to arm, --no-drop swaps LogDropper, waypoint file lat,lon; MavIO now takes baud= for the serial link). Did NOT touch basestation/ per laptop claim. REGRESSION: both SITL scenarios still PASS after the mavlink_io/detector/dropper edits.
